# Journal des décisions — Estate in Paris

Historique daté des choix produit, UI et techniques, sorti de `CLAUDE.md` le 2026-09-22 pour que celui-ci ne garde que les règles et la carte du code. **Toute nouvelle décision utilisateur se note ici** (date, composant, ce qui a été essayé et écarté). Les entrées ci-dessous sont reprises telles quelles.

## Décisions par composant / fichier

### `app/Domain/Blog/`

blog Sanity — **projet partagé avec Relocation in Paris (`ks9vwq45`)** : chaque site a ses types (`blog`/`author`/`category` pour Relocation, **`estateBlog`/`estateAuthor`/`estateCategory`** pour Estate in Paris, mêmes champs) ; le type lu est `SANITY_BLOG_TYPE` (défaut `estateBlog` ; **en anglais la section s'appelle « Blog », pas « Journal »** — décision utilisateur 2026-09-15, param GROQ `$type` via `ListBlogPosts::typeParams()`). Support/SanityClient (GROQ via HTTP, token serveur, cache 5 min, CDN si SANITY_USE_CDN), SanityImage (ref → URL cdn + srcset + dimensions), PortableText (normalise le body, extrait FAQ), SeoText (title : entier avec suffixe si ≤ 60, sinon coupé au dernier séparateur « : ? - | ( » si la tête reste ≥ 30 avec suffixe, sinon **titre entier sans suffixe** (`fitsSuffix()` → prop `seo_title_suffix` → `SeoHead withSuffix`), sinon coupe sur un mot à 60 ; description ≤ 160) ; Actions ListBlogPosts / ShowBlogPost / ListBlogUrls (sitemap) ; Data BlogQuery / BlogPostSummary / BlogPost / BlogListing

### `app/Http/Controllers/BlogController.php`

/blog (+ ?page, noindex > 1, **404 au-delà de la dernière page**), **/blog/categorie/{category}** ↔ `/en/blog/category/{category}` (route `blog.category`, page catégorie **indexable** : canonical propre, titre `blog.seo_title_category`, description `blog.seo_description_category`, intro GEO `blog.intro_category` (pluriel, nombre d'articles), hreflang/switcher → index du blog de l'autre langue car les catégories ne sont pas mappées entre langues, 404 si catégorie inconnue ou vide ; l'ancien `?category=` est ignoré ; URLs dans `sitemap.blog.xml` via `ListBlogCategoryUrls`) et /blog/{slug} → pages/blog/index.tsx (`max-w-6xl`, en-tête centré eyebrow + h1 `blog.headline` + intro comme la page estimation, **dernier article à la une** en page 1 via `components/blog/blog-featured-post.tsx` (**double carte** reprise du blog Relocation in Paris, décision utilisateur 2026-09-15 : cadre extérieur sable en dégradé `from-background-08 to-background-05` + `border` **+ shimmer de bordure** (même arc lumineux que la carte newsletter : 2 passages au chargement puis immobile, `ring-mask`, `motion-reduce:hidden` — décision utilisateur 2026-09-22, pour faire ressortir le cadre) avec le libellé centré `blog.featured` « Dernier article » en eyebrow, carte intérieure `bg-card` en grille 2/5 texte (`BlogPostMeta`, h2 `text-xl lg:text-3xl`, extrait, lien « Lire l'article ») + 3/5 photo `priority` à droite (première sur mobile) avec `BlogCategoryPill` ; hover = bordure + zoom photo ; pas de badge), puis **filtre par catégorie** `blog-category-filter.tsx` (Figma 712-19437 : onglets en capitales `text-xs tracking-wider` sur une hairline, **un seul soulignement qui glisse** (`li` absolu positionné par variables CSS `--indicator-x/-w` mesurées sur l'onglet actif, optimiste au clic via `data-active`, `aria-current` suit le serveur), badge de compte `rounded-full` (`bg-background-08`, actif `bg-primary`), liens `?category=slug` avec `aria-current` ; mobile : bande `overflow-x-auto snap-x` sans scrollbar, fondu à droite, onglet actif ramené en vue par `scrollIntoView`) — côté serveur `BlogQuery::category` (slug validé, sinon ignoré), `ListBlogPosts` ajoute `CATEGORY_FILTER` et remonte dans la même requête `categories` (`SANITY_CATEGORY_TYPE`, défaut `estateCategory`, avec `count` par catégorie, celles à 0 exclues) + `all`, prop `filter` {categories, total_all, active, urls} du contrôleur ; page filtrée = `noindex, follow`, pagination conserve `category`, l'article à la une = **dernier article de la langue, quel que soit le filtre** (clé GROQ `featured`, prop `featured` distincte de `posts`, retiré de la grille par id ; page 1 uniquement) ; les onglets sont des `Link preserveState` (sinon Inertia remonte la page et le trait saute au lieu de glisser) ; filtre + grille dans un bloc `gap-6` ; puis grille des autres (`gap-6`) ; pagination `blog-pagination.tsx` (précédent / numéros avec `aria-current` et ellipses via `pageItems()` / suivant, flèches désactivées en bout) ; pas de fil d'Ariane visible — décision utilisateur 2026-09-15), show.tsx (**page article**, décision utilisateur 2026-09-15 : JSON-LD `Article` complet (`ImageObject` avec dimensions, `inLanguage`, `articleSection`, `keywords`, `wordCount` calculé par `PortableText::wordCount()`, auteur avec `image` + `worksFor`) + **`organizationNode()`** pour résoudre `publisher` (le graphe complet n'est que sur la home) + breadcrumb + FAQPage ; `max-w-6xl`, fil d'Ariane discret (`SeoBreadcrumbs` : `text-xs` atténué, chevrons `size-3`) remonté dans le padding du layout (`-mt-6 sm:-mt-8 lg:-mt-12`) en haut à gauche, en-tête centré comme le listing (pilule catégorie, h1 `text-3xl sm:text-4xl`, extrait en intro), photo principale pleine largeur sans cadre ni légende (**bord à bord sur mobile** `-mx-6` en 16/9 comme la première photo de Vendre, 2/1 dès `lg` — décision utilisateur 2026-09-22 ; la description reste dans l'`alt`), puis grille `lg:grid-cols-3` : colonne gauche `lg:sticky top-24` de **cartes latérales `blog-side-card.tsx`** (même cadre que le récap estimation : `border-secondary-30 bg-card p-2 shadow-lg shadow-black/5` + dégradé sable) : sommaire `blog-toc.tsx` (entrées = sections titrées via `lib/blog-toc.ts`, ids = `_key` des sections dans `blog-body.tsx` + `scroll-mt-24`, entrée active par IntersectionObserver → `aria-current="location"` + `bg-background-08` + filet primaire (hover `bg-background-05`), liste `max-h-72 overflow-y-auto` avec l'entrée active ramenée en vue **en déplaçant le `scrollTop` de la liste, jamais `scrollIntoView`** (sur mobile il faisait remonter la page jusqu'au sommaire — bug rencontré 2026-09-15)) puis `blog-author-card.tsx` (variante ui.sh « Lignes à icônes » : avatar 48px + nom (« Par » en sr-only), `<dl>` Publié le / Mis à jour le si différent / Lecture avec icônes `CalendarDays` / `RefreshCw` / `Clock`) — décision utilisateur 2026-09-15 — et corps sur 2 colonnes ; corps `max-w-prose` ; rythme `portable-text` : p `my-5 text-base/7`, h2 `mt-14 tracking-tight`, h3 `mt-10`, listes sans fond (puits gris essayé puis retiré), sans puces navigateur (`before:` : point `bg-primary` pour les puces, disque sable numéroté par compteur CSS pour les listes ordonnées), citation = **double carte** (variante ui.sh « Double carte », décision utilisateur 2026-09-15) : bloc sable `bg-background-08 p-2`, carte intérieure `bg-card` avec icône `Quote` sable **dans un carré blanc à liseré sable `size-9`** à gauche du texte `text-base/7 sm:text-sm/6` (ni italique ni filet ; variante ui.sh « Icône en tuile blanche » choisie parmi 10 le 2026-09-22 : trait sable, centrée Montserrat, hairlines, filigrane, carte du site, bande sable, point sable, fond primaire écartés), liens soulignés + `hover:text-primary`, externes `target=_blank` + `ArrowUpRight` + sr-only ; vidéos = façade `youtube-embed.tsx` (affiche ytimg + bouton lecture, iframe nocookie chargée au clic ; légende avec la même icône `Info` que les images, icône et texte alignés `items-center` — décisions utilisateur 2026-09-22) ; section Sanity `background` (conclusion / « à retenir ») = **même double carte que la réponse rapide** (bloc sable `p-2`, h2 `text-lg font-medium`, carte intérieure `bg-card`) — décision utilisateur 2026-09-15 ; CTA = `blog-cta.tsx` (carte liseré sable **tout centré** — variante ui.sh « Centré », décision utilisateur 2026-09-15 : avatar `seo.advisor` 64px, surtitre `blog.cta_eyebrow`, titre `text-xl text-balance`, texte, bouton plein `size="lg"` → contact) ; pied d'article : hairline + tags `blog-tags.tsx` en chips **à angles droits** `border-border rounded-none text-xs` (variante ui.sh « Contour carré », décision utilisateur 2026-09-15 ; lien « Tous les articles » et barre de partage retirés) ; `reading-progress.tsx` = hairline `fixed top-0 z-60 h-0.5` (`role=progressbar`, largeur via `--progress` mesurée sur l'`<article>`) (les liens « Article précédent / suivant » ont été construits puis retirés, décision utilisateur) ; **« Nos derniers articles »** `blog-related.tsx` (prefetch `router.prefetch` à l'entrée en viewport via IntersectionObserver, pas de survol sur mobile) = prop `related` (contrôleur : `ListBlogPosts` derniers articles, article courant exclu, 3 max ; mobile = bande `overflow-x-auto snap-x` cartes à 85vw avec `scroll-px-6` (sans lui `snap-start` collait la carte au bord de l'écran en ignorant le `px-6`, bug 2026-09-22), grille dès `sm`) — décision utilisateur 2026-09-15 ; images du corps (`portable-text`) **toutes en 16/9** (`aspect-video object-cover`) avec `figcaption` = alt précédé d'une icône `Info` (décisions utilisateur 2026-09-15) ; bloc « réponse rapide » = **double carte** (h2 précédé d'une **icône lucide `Zap` dans un carré blanc `bg-card` à liseré sable `size-9`** ; la conclusion `background` a la même tuile avec `BookmarkCheck` ; titre et icône en `px-2`, alignés sur le bord de la carte intérieure plutôt que sur son padding — décisions utilisateur 2026-09-22) comme le détail d'article Relocation in Paris (décision utilisateur 2026-09-15) : bloc sable `bg-background-08 p-2` portant le h2 `text-lg font-medium` (titre du bloc ou « Réponse rapide »), puis le texte dans une carte intérieure `bg-card p-6` ; pas de surtitre ; bloc tableau = `blog-table.tsx` (Figma 712-21382 sans arrondi : en-tête `bg-grey-5` `text-xs font-medium` muted `h-11`, lignes `bg-card h-17`, 1ʳᵉ colonne muted, valeurs `text-text-heading font-medium`, bordures `border-border`, `overflow-x-auto`) ; bloc FAQ du corps = `blog-faq.tsx`, **même accordéon que la page FAQ** (`ui/accordion`, h2 + « Tout ouvrir / Tout replier » via `faq.expand_all/collapse_all`, 1ʳᵉ question ouverte, réponses Sanity en texte avec `whitespace-pre-line`) — décision utilisateur 2026-09-15). Blocs partagés carte / à la une : `blog-post-meta.tsx` (avatar auteur + « Par » sr-only + nom · point `bg-muted-foreground` · date courte, repli nom de l'agence) et `blog-category-pill.tsx` (pilule verre + point couleur Sanity). Le switcher/hreflang suivent le slug traduit (translation.metadata) via LocalizedUrls::override()

### `resources/js/components/blog/`

types.ts (miroir des DTO + blocs Sanity), portable-text (block/image/youtube, listes regroupées, marks), blog-body (quickAnswerBlock, wysiwygBlock, tableBlock, ctaBlock, faqBlock), blog-post-card (décision utilisateur 2026-09-15, choisie parmi 10 variantes ui.sh : cadre `border` léger (hover = bordure qui fonce en transition, pas de trait souligné sur les liens), photo en retrait `p-2` du cadre, pilule catégorie `rounded-full` « verre » (`bg-background/60 backdrop-blur-md ring-white/50`) sur la photo avec **point de la couleur Sanity de la catégorie** (`category.color` = `color.hex` validé côté PHP, exposé en variable CSS `--category-color`, repli `bg-primary`), sous la photo **avatar de l'auteur** (`authors[].photo` = `SanityImage::thumbnail()` 64px carré) + nom · date courte (`formatDate(…, 'short')` = mois abrégé) en `text-xs`, sans icônes ni temps de lecture (repli nom de l'agence sans auteur), titre `text-lg line-clamp-2 min-h-14` (cartes alignées), extrait `line-clamp-3 sm:line-clamp-2 text-pretty`, lien « Lire l'article » + `ArrowRight` discret (`text-muted-foreground` → foreground au hover de la carte) ; hover = bordure qui fonce + zoom photo `scale-105` 700 ms ; voile `from-black/25` en haut de la photo pour la lisibilité de la pilule)

### `app/Domain/Valuation/`

**Checkup 2026-09-22 (décisions utilisateur)** : sous la case de consentement, une ligne RGPD `estimate.consent_privacy` (durée de conservation 3 ans + lien `[Politique de confidentialité](privacy)` rendu par `FaqAnswer`, hors du `<label>` pour que le lien ne coche pas la case, reliée par `aria-describedby`) ; le **brouillon** `sessionStorage` n'enregistre plus nom, e-mail ni téléphone (ordinateur partagé) ; **JSON-LD `serviceNode()`** (Service gratuit, provider = organisation, areaServed Paris, offre à 0 €) sur la page ; **bloc « Comment se déroule une estimation ? »** `components/estimate/estimate-process.tsx` sous le formulaire (h2 en question, intro GEO, 3 étapes numérotées sable avec leur délai) ; la clé `estimate.subtitle` orpheline est supprimée ; **flash dédié** `estimate_success` → `flash.estimate` (comme la newsletter, plus de partage de `success` avec le contact) ; **mails en échec** : `report($e)` avec contexte dans `SendValuationRequest` + commande `valuations:resend-mails` (renvoie le mail agence des demandes à `mail_sent_at` null, `Valuation::fromModel()`, planifiée chaque heure, test `ResendValuationMailsTest`) ; délai unifié « sous 24 h ouvrées » (`trust_delay`).

demande d'estimation (Figma 696-13105) : Data/Valuation (DTO, consts PROPERTY_TYPES apartment/duplex/studio/mansion/house/loft/building/other (« dernier étage » n'est pas un type : il est dans FLOORS) et CONTACT_METHODS phone/whatsapp/email), Actions/SendValuationRequest (stocke puis mail agence `Mail/ValuationRequestMail` + confirmation `Mail/ValuationConfirmationMail` dans la langue du propriétaire, `mail_sent_at` null si échec), Models/ValuationRequest (table valuation_requests). `EstimateController` show/store (route `estimate` GET+POST, `throttle:estimate` 5/min, `EstimateRequest` : téléphone 6-15 chiffres (E.164, même règle que le contact ; `PhoneInput` `limitMaxLength` : pas plus de chiffres que la longueur du pays ; sélecteur d'indicatif : groupe « Principaux » (`MAIN_COUNTRIES` FR/BE/CH/LU/MC/GB/US/AE via `countryOptionsOrder` + séparateur `|`) puis « Tous les pays » — décision utilisateur 2026-09-15), surface 5-999 m², chambres ≤ pièces, message ≤ 2000, honeypot `website`, consentement) → `pages/estimate.tsx` = eyebrow + h1 `estimate.headline` + intro GEO + `components/estimate/estimate-form.tsx` (**pas de carte** : formulaire nu à gauche (`lg:col-span-3`), à droite `estimate-recap.tsx` = carte **sticky** (`lg:top-24`, liseré sable, sans ombre) qui récapitule en direct chaque champ saisi (en-tête : avatars des 3 conseillers + titre ; **jauge** : hairline `role="progressbar"` sous l'en-tête (champs requis remplis, largeur via variable CSS `--progress` — seul `style=` dynamique toléré), pastille de groupe → coche verte `animate-pop` quand le groupe est complet ; 5 groupes numérotés (questions des étapes, séparés par des `GradientHairline`) avec libellés courts `estimate.recap_labels.*`, une ligne par champ, pièces + chambres fusionnées, réponses courtes du groupe 3 sur deux colonnes séparées par un trait (`short`), atouts = nombre coché (coche verte + chiffre, pas de pastille) (noms en `sr-only` + `title`), ✓ vert quand rempli, sans compteur, `aria-live="polite"`, un `<dl>` par groupe) — décision utilisateur 2026-08-27 ; 5 étapes numérotées `step-heading` (titre gris → couleur titre quand l'étape est complète) séparées par des `GradientHairline`, **guidage** : choisir le type de bien focalise « Nom complet », choisir le mode de contact focalise la note ; **micro-validation positive** : coche verte `Valide` (sr-only) sur e-mail / téléphone (`isValidPhoneNumber`) / adresse dès qu'ils sont valides ; cartes `active:scale-98` + point radio `animate-pop` ; stepper : boutons −/+ `rounded-none`, valeur animée en **rouleau de slot** (`animate-slot-in/out-up/down` dans `app.css`, direction selon le sens ; overlay `aria-hidden` masqué au focus de l'input pour garder la saisie visible ; état `from` gardé en `useState`, pas en ref — décision utilisateur 2026-09-15) ; **mobile** : récap masqué, bouton d'envoi du formulaire masqué (`hidden lg:inline-flex`, un seul bouton par écran), barre `sticky bottom-0` en fin de colonne formulaire (`lg:hidden`, colle en bas de l'écran puis part avec la section) avec le bouton d'envoi (`form=`) + bouton flèche (▲/▼, contrôlé) ouvrant le récap **sans cadre** (`frameless` : même fond `bg-card` + dégradé sable que la carte desktop, sans bordure ni ombre) dans un `Sheet` bas (voile `bg-black/30` maison au-dessus de la page, un tap le ferme) qui s'arrête au-dessus de la barre (`bottom-[calc(4.5rem+safe-area)]`, `hideClose`, fermé automatiquement au passage en desktop (`matchMedia(min-width: 64rem)`) ; la barre reste visible et cliquable : `Sheet modal={false}` (pas d'overlay ni d'`aria-hidden` sur la page), `z-60` + `onPointerDownOutside` ignoré sur la barre ; `ui/sheet.tsx` : `shadow-none`, croix `rounded-none`) ; **à l'ouverture du récap la page est recalée** (`scrollBy` de l'écart entre le bas de la barre et le bas de l'écran, `smooth` sauf `prefers-reduced-motion`) pour que la barre revienne en bas de l'écran quand elle a déjà quitté cette position en fin de formulaire, le sheet étant fixé au-dessus ; **espace `h-10` sous la barre** (elle se décolle de l'écran avant la fin de la colonne au lieu d'épouser la fin de la section) — décisions utilisateur 2026-09-22 ; **envoi incomplet** : pas de requête, premier champ requis manquant (ordre de la page) marqué `estimate.required_hint` + focalisé, flag effacé à la prochaine saisie ; **brouillon** `sessionStorage` `estimate-draft` restauré à l'ouverture (jamais le consentement), effacé après envoi ; `selection-cards` = Radix RadioGroup en cartes (icônes lucide, coché = `bg-background-05 border-secondary-50`, nouveau token), `stepper-input` −/+ (input number réel, pièces 1-10, chambres 0-10 ≤ pièces), étage en `Select` (`Valuation::FLOORS` : RDC, 1-6, 7 et plus, dernier étage) + case ascenseur, atouts multi-choix `Valuation::FEATURES` (lumineux, belle vue, calme, balcon/terrasse, proche métro, hyper centre, parking, cave — colonne JSON `features`) + `Select` état général (`CONDITIONS`, « à rénover » remplace l'ancien « travaux à prévoir »), valeur estimée formatée `1 500 000` (chiffres seuls envoyés), unité m² / € en suffixe, textarea 2000 (le récap n'affiche que « Note ajoutée »), consentement, mode de contact pré-sélectionné selon la langue (EN → e-mail) + indice WhatsApp ; **erreurs serveur** : résumé `role="alert"` en tête (compteur + liens), premier champ en erreur focalisé (`lib/focus-field.ts`) ; récap : chaque ligne est un bouton « Modifier : … » qui focalise le champ ; sous le bouton : Confidentiel · Sans engagement · Réponse sous 24 h + avatar du conseiller `seo.advisor`, `estimate-success` (Figma 696-13309 / 696-13603 : **même carte que le reste du site** (`border-secondary-30 bg-card p-2` + dégradé sable intérieur — le cadre pointillé du Figma a été retiré, décision utilisateur 2026-09-16), badge succès, titre, **référence `VAL-{année}-{id}`** (accesseur `ValuationRequest::reference`, flash `valuation_reference` → `flash.valuationReference`, bouton copier) , carte conseiller `seo.advisor` (photo carrée, chips expérience / langues / délai), 3 prochaines étapes (1 faite ✓, 2 en cours, 3 à venir ; pointillés horizontaux desktop / verticaux mobile), CTA « Découvrir nos biens » → `buy`) remplace formulaire + récap ; la référence figure aussi dans les deux mails). Textes `ui.estimate.*`.

### `app/Domain/Newsletter/`

**Revue 2026-09-22 (décisions utilisateur)** : le paragraphe sous le h1 est l'intro GEO `pages.newsletter.intro` en `text-pretty` (l'accroche `newsletter.subtitle` n'est plus rendue) ; les bénéfices ont un **h2 visible en question** « Que contient la newsletter ? », des icônes qui **restent en disque sable `bg-background-05 rounded-full`** comme les coordonnées de la page contact (un carré blanc à liseré sable puis un carré sable ont été essayés et retirés le même jour) et des textes avec **source nommée** (Notaires du Grand Paris) sans chiffre inventé ; **preuve sociale** = note Google réelle (`seo.reviews`, masquée sans chiffres) à droite de la pilule « pas de spam » + **3 derniers articles** (`BlogRelated`, prop `posts`, masqué si Sanity échoue) sous les bénéfices.

inscription newsletter : Data/NewsletterSubscription, Actions/SubscribeToNewsletter (une ligne par adresse : création, ré-activation si désinscrite ; une adresse déjà active renvoie **le même succès** sans second e-mail — aucune fuite « déjà inscrit », décision utilisateur 2026-08-27), Models/NewsletterSubscriber (table newsletter_subscribers, e-mail unique, subscribed_at/unsubscribed_at). NewsletterController show/store (throttle:newsletter 5/min, honeypot `website`), prop `nextIssue` = lundi courant ou prochain lundi (iso + libellé localisé) ; flash dédié `newsletter_success` → `flash.newsletter` (`flash.success` reste réservé au contact) ; fréquence **hebdomadaire, le lundi** partout (page, llms.txt) → pages/newsletter.tsx (Figma 262-7802 : **fond de page uni comme les autres pages** — le bandeau dégradé sable `w-screen` et les vagues SVG animées du Figma ont été construits puis **retirés**, décisions utilisateur 2026-08-27 / 2026-09-15 ; `<PageEyebrow>` (`components/page/page-eyebrow.tsx`, surtitre = `pages.<key>.title` ; sur contact = `nav.contact_page` « Contactez-nous ») + h1 + phrase-réponse, components/newsletter/newsletter-form (carte `max-w-xl` **au même style que les cartes contact / estimation / blog** : `border-secondary-30 bg-card p-2` + dégradé sable intérieur — décision utilisateur 2026-09-15 ; sous-titre en `text-balance`, e-mail + bouton sur une ligne — label `sr-only` via `FormField hideLabel`, erreur sous la ligne entière via `FormField externalError`, `enterKeyHint="send"` —, avatars + date (pas de ligne de promesse ni de mention d'auteur : retirées, décision utilisateur 2026-08-27), pilule ShieldCheck « pas de spam », shimmer de bordure **2 passages puis immobile** ; succès = titre + date de la 1re édition + CTA blog / accueil) + newsletter-benefits (3 colonnes orientées « ce que je reçois » : opportunités / chiffres du marché / analyses en 3 min, icônes lucide BadgeCheck/TrendingUp/Timer en `size-5` dans un disque `size-11` (décision utilisateur 2026-09-15), hairlines en dégradé). **Bloc d'inscription compact dans le footer** (`components/footer/newsletter-signup.tsx`, même route, même flash) — UX revue le 2026-08-27 (décision utilisateur). E-mail de bienvenue `Mail/NewsletterWelcomeMail` (vues `mail/newsletter-welcome(-text)`, textes `ui.mail.newsletter_*`, langue de l'abonné, `welcome_sent_at` null si l'envoi échoue). Pas encore de lien de désinscription (le mail invite à répondre). Lien dans le footer (`useFooterNavItems`).

### `app/Http/Controllers/AboutController.php`

**Revue 2026-09-22 (décisions utilisateur)** : les h2 sont des **questions** (« Qui sommes-nous ? » manifeste, « Qu'est-ce qui guide nos missions ? » valeurs, « Qui sont nos conseillers ? » équipe — règle GEO) ; **ordre des blocs** = hero → engagements → manifeste → **valeurs** → **équipe** → témoignages → CTA (quoi, pourquoi, qui, preuve, action ; les valeurs passaient après les avis) ; **JSON-LD** : `teamOrganizationNode()` liste les conseillers en `employee` et `teamNodes()` émet un `Person` par membre (`jobTitle`, `image`, `knowsLanguage` déduit des drapeaux, `worksFor`) — à garder tant que l'équipe est fictive, mais à remplir avec les vraies personnes avant mise en ligne ; l'avatar « Votre interlocuteur unique » des valeurs est retiré.

page « À propos » (`/a-propos` ↔ `/en/about-us`, route `about`) → `pages/about.tsx` : `PublicLayout max-w-7xl` (l'équipe prend cette largeur, comme le Figma ; en-tête et carte CTA restent en `max-w-5xl` comme sur la HP — décision utilisateur 2026-09-16), **hero** `components/about/about-hero.tsx` (Figma 712-23809 / 712-23841 desktop, 712-24213 / 712-24243 mobile, décision utilisateur 2026-09-16 : à gauche **un seul bloc centré verticalement** dans la colonne, **aligné à gauche** en desktop (centré sur mobile) = eyebrow `about.hero_eyebrow`, h1 `about.headline`, intro GEO `pages.about.intro`, bouton `about.hero_cta` en ancre vers `#team-title`, puis la ligne de preuve dans le même bloc (une carte et une hairline ont été essayées puis retirées) = 3 portraits ronds des conseillers + étoiles sable + note et nombre d'avis réels `seo.reviews` (masquée sans chiffres) ; à droite (`lg:-mr-3` : bord droit aligné sur le bouton « Nous contacter » du header, dont le conteneur a 20px de marge contre 32px pour le layout — décision utilisateur 2026-09-16) photo **parisienne** `public/images/about/hero-{800,1400}.jpg` (recadrage portrait 4/5 de la façade haussmannienne du hero de la HP ; la photo d'équipe du Figma a été remplacée, décision utilisateur 2026-09-16) (zoom d'entrée `animate-hero-photo` comme la HP) avec **voile court** (`h-72 from-black/60`, la façade reste claire au-dessus) et **carte verre sombre** `bg-black/40 backdrop-blur-md` à angles droits qui fait défiler 3 messages `about.hero_slide_*` (**icône lucide** Award / EyeOff / Globe dans un carré, surtitre, titre h2, texte) avec points verticaux + flèches haut / bas, **↑ / ↓ au clavier** sur les contrôles — pas de défilement automatique, `aria-live` (améliorations 3, 5, 6, 8, 9, 10 de la revue, décision utilisateur 2026-09-16)), puis **bandeau d'engagements** `components/about/values-marquee.tsx` (Figma 712-23866 : bande `bg-background-05` pleine largeur (même sable que le haut du bandeau manifeste qui suit, pour ne faire qu'un), 5 phrases `about.marquee_*` Montserrat capitales `text-sm tracking-wider text-muted-foreground/70` (texte allégé, décision utilisateur 2026-09-16) séparées par des points sable, **défilement infini** `animate-marquee` (piste = deux moitiés identiques de trois listes chacune, une seule liste exposée aux lecteurs d'écran, toutes de largeur identique point de fin et marge compris ; glissement `-50 %` : une moitié dépasse toujours la largeur de l'écran, donc ni fin visible ni saut), **pause au survol** (`group-hover:[animation-play-state:paused]`), immobile et sur plusieurs lignes en `motion-reduce` ; **collé** à la photo du hero au-dessus et au bandeau manifeste en dessous (`-mb-12 lg:-mb-16`, aucun espace) — décision utilisateur 2026-09-16 ; mobile / tablette : en-tête centré puis panneau photo **bord à bord** (`-mx-6`) en **16/9** (4/5 essayé puis remplacé, décision utilisateur 2026-09-16) ; desktop `min-h-[46rem]` — photo agrandie en hauteur, décision utilisateur 2026-09-16), puis **« Notre équipe »** `components/about/team-grid.tsx` (Figma 712-23908 / 712-24310 : en-tête eyebrow + h2 à gauche et intro à droite en desktop (**intro courte, deux lignes max** — décision utilisateur 2026-09-16), centré sur mobile ; 6 personnes `team.members` (`ui.php`, prop `team` du contrôleur), chacune dans la **carte du site** (`border-secondary-30 bg-card p-2` + dégradé sable intérieur, décision utilisateur 2026-09-16 via /ui) = photo **carrée** à angles droits (3:4 dans le Figma, carré sur décision utilisateur 2026-09-16 ; `alt=""`, le nom est juste dessous), nom `text-lg font-medium` (les capitales du Figma retirées), puis **hairline en dégradé, nom + rôle alignés à gauche, langues « FR & EN » en texte dans un chip sable carré à droite** (drapeaux essayés puis retirés ; la carte a ensuite été confirmée parmi 15 variantes ui.sh le 2026-09-16) (variante ui.sh « Aligné à gauche, hairline, chip langues = **drapeaux** (`CountryFlag` FR + GB depuis `team.members[].flags`, libellé `languages` « Parle français et anglais » en sr-only — décision utilisateur 2026-09-16, plus de texte « FR & EN ») », décision utilisateur 2026-09-16) ; grille 3 colonnes en desktop, **une carte centrée à la fois** glissable à la souris + flèches sur mobile / tablette (même mécanique que les services) ; le bouton « Voir toute l'équipe » du Figma n'a pas de destination et a été laissé de côté ; portraits `public/images/team/member-{1..6}.jpg` = **placeholders exportés du Figma, à remplacer par la vraie équipe**), puis le **bloc témoignages de la home** (`components/home/testimonials.tsx`, prop `testimonials`, bandeau **pleine largeur d'écran** via `relative left-1/2 w-screen -translate-x-1/2` + `overflow-x-clip` sur la racine de `PublicLayout` (pas sur la colonne 7xl : elle couperait le bandeau) — décision utilisateur 2026-09-16), puis **« Nos valeurs »** `components/about/values-list.tsx` (mise en page du Figma 712-24134 / 712-24468 « processus de vente », transformée en bloc de valeurs sur décision utilisateur 2026-09-16 : colonne gauche `lg:sticky` = en-tête `values.*` + bouton contact (l'avatar du conseiller « Votre interlocuteur unique » a été retiré, décision utilisateur 2026-09-22) ; à droite `<ol>` de 5 valeurs reliées par un **fil vertical** `GradientHairline vertical` avec un point sable par valeur, grand numéro Montserrat sable `text-4xl` + titre sur la même ligne dès le mobile, phrase dessous, hairlines entre les valeurs ; la valeur au **milieu du viewport est active** (numéro et point foncés ; **mesurée au scroll** = valeur sous la ligne médiane, sinon la plus proche — l’`IntersectionObserver` sur une bande de 10 % ratait la valeur la plus courte « Réactivité » qui la traversait entre deux rappels, bug 2026-09-21 ; **verrouillage** `animate-value-lock` du numéro + anneau `animate-value-ring` du point à chaque changement, `active` vaut `null` avant le premier scroll — décision utilisateur 2026-09-21 ; **chaque valeur est aussi un bouton** (`aria-current` sur l'active, hover `bg-background-05` + numéro `secondary-60`, focus-ring) qui l'active au clic / Entrée en plus du scroll, comme les stratégies de la page Acheter — décision utilisateur 2026-09-21), **entrée en cascade** `animate-hero-rise` + `--stagger` 80 ms à l'arrivée dans le viewport (`motion-reduce` annule) ; **sur le fond blanc de la page, sans bandeau ni dégradé** — un bandeau sable a été essayé puis retiré, décision utilisateur 2026-09-16), puis la **carte CTA de la home** (`components/home/cta-card.tsx`) ; les 4 sections de contenu (qui / comment / conseiller / où) ont été construites puis **supprimées** (décision utilisateur 2026-09-16) ; JSON-LD `AboutPage` + `organizationNode` + breadcrumb ; sitemap, llms.txt, `SeoMetaLengthTest`, `AboutTest` ; **manifeste + chiffres clés** `components/about/agency-manifesto.tsx` (Figma 712-23876 desktop / 712-24278 mobile, décision utilisateur 2026-09-16) juste après l'en-tête : bande sable `from-background-05 to-background from-40%` (bandeau du site ; le `bg-background-10` plat a été revu, décision utilisateur 2026-09-16) en breakout `w-screen` comme les témoignages, h2 `about.manifesto_title`, deux phrases Montserrat `text-xl sm:text-2xl lg:text-3xl` (marque `about.manifesto_brand` en semibold + `manifesto_1` = phrase GEO, `manifesto_2`), pictogramme clé `brand/logo-mark.svg` en filigrane `opacity-20` (même glyphe que le vecteur Figma) — **révélation premium** à l'entrée dans le viewport (décision utilisateur 2026-09-16) : le texte **s'écrit mot à mot** (chaque mot du titre puis des deux phrases est un `span inline-block` décalé de 40 ms, le pictogramme ferme la séquence) depuis une légère bascule 3D `rotateX` avec un flou qui se dissipe (`animate-manifesto-in` dans `app.css`, `--stagger`, IntersectionObserver, `motion-reduce:animate-none`), puis grille de 4 chiffres `about.stats` {value,title,text} (prop `stats` du contrôleur ; **chiffres réels uniquement** ; titres en forme nominale « Transactions réalisées ») : empilés sur mobile, **2×2 dès `sm`**, 4 colonnes dès `lg`, hairlines sable en dégradé entre (horizontales `GradientHairline`, verticales en pseudo-élément `before:` sur les colonnes, jamais les pointillés du Figma) ; valeur `text-3xl sm:text-4xl font-semibold` animée en **compteur** (`StatValue` : préfixe / chiffres / suffixe parsés depuis la chaîne, 1,4 s expo-out à l'entrée de la grille dans le viewport, valeur finale en `sr-only` et valeur animée `aria-hidden` — `aria-label` sur un span est refusé par axe), h3 `text-lg font-medium` + texte muted qui montent en cascade (`animate-hero-rise`, 120 ms) ; `prefers-reduced-motion` = chiffres finaux immédiats ; pictogramme en `opacity-30 h-11` ; `manifesto_1` ancre la marque avec « rue Grégoire de Tours à Paris 6e » (pas d'année de fondation : aucune donnée réelle) — décisions utilisateur 2026-09-16 ; tests `AboutTest` + `agency-manifesto.test.tsx`. **Gouttières** (audit 2026-09-16) : toute section / bande = `px-6 lg:px-8` (comme `PublicLayout`), les rangées à défilement horizontal (services, équipe, témoignages) débordent avec `-mx-6` + `w-[calc(100%+3rem)]` et reviennent à `lg:mx-0` ; **hero** `components/about/about-hero.tsx` (Figma 712-23809 / 712-23841 desktop, 712-24213 / 712-24243 mobile) : en-tête + photo d'équipe avec carte verre et carrousel de messages sans autoplay — **switcher = 3 petits points serrés, sans flèches** (`ul` de boutons `size-6`, flèches clavier sur le groupe) et **glissement vertical sur la carte** à la souris (capture du pointeur, pas de sélection) ou au doigt (≥ 40px : vers le haut = suivant, vers le bas = précédent ; `touch-pan-x`, `cursor-grab`) et **molette** (un cran = un message, verrou 600 ms, écouteur non passif qui bloque le scroll de la page au-dessus de la carte) — décision utilisateur 2026-09-16 (**transition rouleau** : l'ancien message glisse dans le sens de navigation en se floutant pendant que le nouveau entre par l'autre côté et se nettifie — `animate-reel-in/out-up/down` dans `app.css`, `from` en `useState` comme le stepper, copie sortante `aria-hidden` retirée à `animationend`, `motion-reduce:hidden` ; (un décalage de la carte verre + parallaxe de la photo pendant le rouleau a été essayé puis **retiré**, l'utilisateur n'a pas aimé — 2026-09-16) ; suivant = vers le haut, précédent = vers le bas, points selon l'index ; le **fond verre suit la hauteur** du message entrant (mesure ResizeObserver → variable `--card-h`, `transition-[height]` 500 ms, seul `style=` dynamique toléré) — décision utilisateur 2026-09-16) ; **photo à fleur** du header et de la bande manifeste (marges négatives `-mt-10 sm:-mt-12 lg:-mt-20 -mb-12 lg:-mb-16` (padding haut du layout réduit à 40px mobile / 48px tablette le 2026-09-22), colonne texte avec son propre padding) et **pas de façade SVG** derrière (`PublicLayout backdrop={false}`) — décisions utilisateur 2026-09-16

### `app/Http/Controllers/BuyController.php`

**2026-09-22, hero partagé** : le hero Acheter est extrait dans `components/page/service-hero.tsx` (props `id`, `texts`, `href`, `photo {src {w}, widths}`, `stats`, `video`, `ref`) ; `buy-hero.tsx` et `sell-hero.tsx` sont des wrappers fins. **Vendre prend exactement le header d'Acheter** (décision utilisateur) : eyebrow « Vendre à Paris », h1 « Vendez votre bien de prestige à Paris au juste prix », intro GEO + bouton plein « Faire estimer mon bien » → estimation, photo `images/sell/photo-*` avec la carte verre des chiffres clés (prop `stats`, `video` ← `SEO_VIDEO_SELL`) ; la ligne de confiance et la ligne de preuve ajoutées plus tôt dans la journée sont retirées avec, `SellPhoto` supprimé, `SeoHead` direct dans `pages/sell.tsx`. Le bloc `FeatureSplit` de la vente confidentielle a été ajouté sur **Acheter** côté acquéreur (« Comment accéder aux biens qui ne sont jamais publiés ? ») puis **supprimé dans la foulée** (décision utilisateur 2026-09-22).

**2026-09-22, hero Acheter** : le hero s'adresse à qui veut acheter dans la capitale, pas à l'investisseur — eyebrow « Acheter à Paris », h1 « Trouvez l'appartement ou l'hôtel particulier qui vous ressemble à Paris », bouton « Parler à un conseiller » (« Investir à Paris / Bâtissez un patrimoine durable / Découvrir nos opportunités » retirés, décision utilisateur). Le bloc stratégies garde son angle investissement.

**2026-09-22, conversion** : sous le bouton plein du hero Vendre, la ligne « Confidentiel · Sans engagement · Réponse sous 24 h ouvrées » du formulaire d'estimation (`sell.trust_*`, icônes Lock / FileCheck / Clock) lève l'objection au moment du clic ; les **cartes de quartier** sont extraites dans `components/page/district-cards.tsx` (props `id`, `texts`, `items`), `buy-districts.tsx` n'est plus qu'un wrapper, et la page Vendre les affiche entre la méthode et la galerie sous le h2 « Combien vaut le m² dans votre quartier ? » (`sell.districts.*`, prop `districts` via `ListDistricts`) — décisions utilisateur.

page « Acheter » (`/acheter-immobilier-paris` ↔ `/en/buy-property-paris`, route `buy`) → `pages/buy.tsx` (`PublicLayout max-w-7xl`, `SeoHead` + breadcrumb JSON-LD, pas de fil d'Ariane visible) = **hero** `components/buy/buy-hero.tsx` (Figma 712-18453 desktop / 712-18766 mobile, implémenté puis revu en UI le 2026-09-21, décisions utilisateur) : en-tête = eyebrow `buy.eyebrow` + h1 `buy.headline` (**deux lignes max**, `max-w-3xl`, `lg:text-5xl`) à gauche, intro GEO `pages.buy.intro` + bouton `buy.cta` (`ArrowUpRight`) → contact dans une colonne **étroite** (`lg:max-w-sm`) à droite, alignés en bas (`lg:items-end`, `lg:gap-24`), tout centré et empilé sur mobile ; puis **panneau photo** = **la photo du hero de la home** `public/images/home/hero-*.jpg` pour l'instant (l'export Figma « intérieur vue tour Eiffel » reste dans `public/images/buy/hero-{800,1600}.jpg` sans usage), `priority`, zoom d'entrée `animate-hero-photo`, angles droits sans cadre (le `rounded` du Figma ignoré), **bord à bord sur mobile** (`-mx-6`) en 16/9, **21/9 en desktop** ; si une vidéo est configurée (`config/seo.php › videos.buy` ← `SEO_VIDEO_BUY`, id YouTube, prop `video`) un **bouton lecture blanc** `bg-background` rond au centre (façade : l'iframe `youtube-nocookie` autoplay ne se charge qu'au clic, comme `youtube-embed.tsx` ; sans vidéo → pas de bouton) ; **chiffres clés** = prop `stats` = `about.stats` (mêmes 4 chiffres réels que la page À propos) en `<ul role=list>` valeur d'abord (`text-xl`, `lg:text-2xl` sur la photo) + libellé `text-xs` (**volontairement discrets**, décision utilisateur 2026-09-21), **compteur** `StatValue` (extrait dans `components/page/stat-value.tsx`, partagé avec le manifeste À propos) + cascade `animate-hero-rise` 120 ms à l'entrée du panneau dans le viewport (IntersectionObserver) : en desktop dans une **carte verre sombre** `bg-black/40 backdrop-blur-md` à angles droits posée en bas de la photo (`inset-x-10 bottom-10`) sur un voile court (`h-[30%] from-black/60`), hairlines verticales en dégradé blanc `via-white/60` comme le hero home ; sur mobile / tablette (et en desktop pendant la lecture vidéo) **grille 2×2** sous le panneau avec une `GradientHairline` entre les deux rangées ; puis **« Pourquoi acheter avec Estate in Paris ? »** `components/buy/buy-advantages.tsx` (Figma 712-18480 desktop / 712-18794 mobile, implémenté puis **amélioré le 2026-09-21** sur décision utilisateur « fait tout ») : **bandeau sable pleine largeur** (`from-background-05 to-background from-40%` en breakout `w-screen`, comme les témoignages : le gris `#F8F8F8` du Figma devient le sable du site) avec sa propre colonne `max-w-7xl px-6 lg:px-8 py-16 sm:py-20` ; en-tête centré (eyebrow `buy.why_eyebrow`, **h2 en question** `buy.why_title`, intro GEO `buy.why_intro`), puis 4 cartes non interactives `buy.why_{sourcing,analysis,negotiation,international}_{title,text}` (**un fait par carte** : off-market jamais publiés, transactions réelles du quartier, réponse sous 24 h ouvrées, visites à distance / procuration / financement FR + EN — la carte « Accompagnement de A à Z » du Figma doublonnait les services de la home et a été remplacée par « Acheteurs internationaux ») dans la **carte du site** (`border-secondary-30 bg-card p-2` + dégradé sable, angles droits : les tuiles blanches arrondies du Figma sont adaptées à la DA) = ligne du haut icône lucide (House / ChartLine / UserCheck / Globe) dans un carré `size-11` + **numéro sable Montserrat `01`-`04`** (comme les valeurs À propos), **filigrane sable de l'icône** en coin toujours visible à 25 % (comme les services), h3 `text-base font-medium` + phrase `text-sm/6` (textes **réduits** sous l'échelle habituelle, décision utilisateur 2026-09-21) poussés en bas (`justify-between`, `min-h-56 lg:min-h-72`) ; **survol** = même retour que les services (liseré `secondary-50`, carré d'icône `bg-primary`, filigrane à 70 %, couleurs seulement — décision utilisateur 2026-09-21) ; **entrée en cascade** `animate-hero-rise` + `--stagger` 80 ms à l'arrivée de la grille dans le viewport ; **empilées sur mobile comme le Figma** (pas de carrousel), 2×2 dès `sm`, 4 colonnes dès `lg` ; sous la grille un bouton `outline` `buy.why_cta` « Parler à un conseiller » → contact (seul élément focalisable)  ; puis les **stratégies** `components/buy/buy-strategies.tsx` (prop `strategies`) ; puis **« Dans quels quartiers de Paris investir ? »** `components/buy/buy-districts.tsx` (Figma 712-18574 desktop / 712-18884 mobile, 2026-09-21) : en-tête centré (eyebrow `buy.districts.eyebrow`, h2 en question, intro GEO), 4 cartes de quartier = prop `districts` (`buy.districts.items` {name, area, text, price, tags[], photo_alt}) **dans le style de la carte blog** (décision utilisateur 2026-09-22, variante ui.sh « Carte blog » choisie parmi 5 : la mise en page « maison de luxe » du Figma (photo nue, capitales espacées, hairline) n'a pas plu, une carte du site à liseré sable, un voile « success stories » et des lignes éditoriales numérotées ont été écartés) = cadre `border-border` léger (hover = bordure qui fonce + zoom photo `scale-105` 700 ms), photo 3/2 en retrait `p-2` avec l'**arrondissement incrusté** dans le coin haut gauche de la photo (tuile blanche `bg-card` à angles droits `top-2 left-2`, Montserrat `text-xs uppercase tracking-wider`, `aria-hidden` — demande utilisateur 2026-09-22, remplace la pilule verre), puis `p-5 pt-3` : h3 = **épingle `MapPin` dans un carré `size-9` à liseré sable **sur fond sable `bg-background-08` (comme la FAQ, demande utilisateur 2026-09-22)** (tuile d'icône des services, demande utilisateur 2026-09-22) + quartier `text-lg font-medium` (arrondissement + virgule en `sr-only` dans le h3), phrase `text-base/7 sm:text-sm/6` muted, phrase `line-clamp-2` (sans hauteur réservée ni `mt-auto` : la marge entre la phrase et les chips était jugée trop grande, décision utilisateur 2026-09-22), **ligne de faits** juste sous la phrase (`gap-3` + `pt-2`, ajusté deux fois le 2026-09-22 : `mt-auto` + `pt-3` trop large, `gap-3` seul trop collé) = **une rangée de chips contour carrés** `border-border px-2 py-0.5 text-xs` comme les tags du blog, le prix moyen en dernier chip `text-text-heading font-medium tabular-nums` (`buy.districts.price_label` en sr-only) avec un **shimmer discret** `animate-sweep-shimmer` (bande sable en `span aria-hidden` absolu, chip `relative overflow-hidden`, décision utilisateur 2026-09-22) — variante ui.sh « Rangée de chips, prix compris » choisie parmi 15 le 2026-09-22 (points, chips sable, pilules, hairline + capitales, prix en grand, `dl`, coches vertes, badge plein… écartés ; un prix à droite sous libellé visible a été essayé puis **annulé** le même jour, décision utilisateur) ; **source datée** `buy.districts.price_source` (Notaires du Grand Paris, année) affichée une fois sous la grille en `text-xs` muted et reliée à chaque prix par `aria-describedby` (règle GEO) ; **entrée en cascade** `animate-hero-rise` + `--stagger` 80 ms à l'arrivée de la grille dans le viewport, comme les avantages ; incrustation collée au coin `top-2 left-2` (un retrait `top-4 left-4` a été essayé puis annulé, décision utilisateur 2026-09-22) ; `sizes` 36rem dès `80rem`; grille `gap-6 sm:grid-cols-2 lg:gap-8` ; toute la section sur le **dégradé sable → blanc du Figma** (`from-background-05 to-background to-60%`) en breakout `w-screen` avec sa colonne `max-w-7xl px-6 lg:px-8 py-16 sm:py-20`, **sans le SVG de vagues** (décision utilisateur 2026-09-21); **empilées sur mobile**, **2 colonnes dès `sm`** de cartes égales (le rythme large / étroite du Figma donnait des photos de hauteurs différentes, décision utilisateur 2026-09-21) ; rien d'interactif (pas de page quartier) ; photos `public/images/buy/district-{1..4}-{800,1600}.jpg` = **placeholders exportés du Figma** (16e = Trocadéro, la photo « famille au jardin » du Figma n'étant pas parisienne) ; **prix ≈ arrondis et année de `price_source` à vérifier avec les derniers chiffres des notaires avant chaque release** (règle « chiffres réels ») ; tests `BuyTest` + `buy-districts.test.tsx` ; les sections de la page sont espacées par `gap-20 sm:gap-28` sur `PublicLayout`; puis **« Des stratégies conçues autour de vos objectifs »** `components/buy/buy-strategies.tsx` (Figma 712-18511 desktop / 712-18821 mobile, implémenté 2026-09-21) : en-tête centré (eyebrow `buy.strategies.eyebrow`, h2, intro GEO), puis grille 2 colonnes dès `lg` (`lg:items-center lg:gap-20`) = à gauche **une photo par stratégie** `public/images/buy/strategies-{1,2,3}-{800,1600}.jpg` (**placeholders** : 1 = terrasse vue tour Eiffel exportée du Figma, 2 et 3 = copies des photos des success stories, à remplacer par trois sujets distincts) qui **se fondent l'une dans l'autre en desktop** pour suivre la stratégie active (`transition-opacity duration-700`, toutes empilées en absolu, la 1ʳᵉ seule visible sous `lg` (`hidden lg:block` sur les autres) et en `motion-reduce` ; seule la 1ʳᵉ porte l'`alt`, les autres `alt=""`) — décision utilisateur 2026-09-21 ; nue, angles droits, 16/9 sur mobile et **carrée en desktop**, sans texte ni voile — la phrase et les 3 points du Figma ont été retirés, décision utilisateur 2026-09-21 ; à droite les 3 stratégies `strategies.items` (prop `strategies` du contrôleur) **numérotées comme les valeurs À propos** (variante ui.sh « Numéros sable » choisie parmi 4 le 2026-09-21, décision utilisateur : la carte du site aurait doublonné les cartes avantages juste au-dessus) = `<ol>` avec fil vertical `GradientHairline` + point sable, grand numéro Montserrat sable `01`-`03` `text-4xl`, h3 `text-lg font-medium` + petite icône lucide (TrendingUp / Coins / ShieldCheck) `size-4` sur la même ligne, phrase dessous, espacées `gap-12 lg:gap-14` ; **même verrouillage au scroll que les valeurs À propos** (IntersectionObserver `-45%`, `animate-value-lock` du numéro + anneau `animate-value-ring` du point, rien d'actif avant le premier scroll) ; **chaque ligne est aussi un bouton** (`aria-current` sur l'active, hover `bg-background-05` + numéro `secondary-60`, focus-ring) qui active la stratégie au clic / Entrée en plus du scroll — décisions utilisateur 2026-09-21 ; empilé sur mobile, photo en premier ; le reste de la page reste **à venir**. puis **« Notre bilan »** `components/buy/buy-record.tsx` (Figma 712-19621 desktop / 712-20151 mobile, 2026-09-22) : en-tête eyebrow + h2 à gauche et intro GEO à droite alignée en bas (`lg:flex-row lg:items-end`, empilé et centré sur mobile), puis grille `lg:grid-cols-[2fr_3fr]` = **carte « Notre engagement »** (carte du site avec dégradé sable plein `from-background-08 to-background-05` : h3, phrase Montserrat `text-lg/7 sm:text-xl/8`, 3 portraits des conseillers `size-8`, ligne de preuve = **nombre réel d'avis Google** `seo.reviews.count` via `buy.record.trusted` (pluriel) + `trusted_sub`, masquée sans chiffres — le « 2500+ property owners » du Figma n'a pas de source) + **grille 2×2 des 4 chiffres clés** (prop `stats` = `about.stats`, mêmes chiffres réels que le hero ; valeur Montserrat `text-2xl sm:text-3xl` en compteur `StatValue`, h3 `text-base font-medium`, phrase `text-sm/6`, icône lucide Coins / UserCheck / ReceiptText / FileCheck sable `size-7` en haut à droite) ; toutes les cartes = carte du site (les tuiles arrondies sable / grises du Figma adaptées à la DA), cascade `animate-hero-rise` + `--stagger` 80 ms à l'entrée dans le viewport ; rien d'interactif ; tests `buy-record.test.tsx` + `BuyTest` ; puis un **bloc FAQ** `components/buy/buy-faq.tsx` (décision utilisateur 2026-09-21) = **deux colonnes en desktop comme les valeurs À propos** (`lg:grid-cols-[2fr_3fr] lg:gap-24`, décision utilisateur 2026-09-21) : à gauche, `lg:sticky`, l'en-tête (`buy.faq.eyebrow` / `title` / intro GEO) + le lien `buy.faq.all` ; à droite le **même accordéon que la page FAQ et le blog** (`ui/accordion`, 1ʳᵉ question ouverte, « Tout ouvrir / Tout replier ») sur les **6 premières questions du thème « Acheter un bien »** de la FAQ (prop `faq` {slug, items[{question, answer, slug}]} : `BuyController::faq()` tranche `ui.faq.categories` — une seule source de vérité, réponses avec le mini-markup `[libellé](route)` rendu par `FaqAnswer`), lien → `route('faq')#slug-du-thème`, JSON-LD `faqPage()` ajouté au `SeoHead` de la page (réponses via `stripFaqMarkup`) ; puis la **carte CTA de la home** (`components/home/cta-card.tsx`, `max-w-5xl` comme sur À propos — décision utilisateur 2026-09-21). Tests `BuyTest` + `buy-hero.test.tsx` (clavier : Tab jusqu'au bouton lecture, Entrée → iframe) + `buy-advantages.test.tsx` + `buy-strategies.test.tsx` + `buy-faq.test.tsx`

### `app/Http/Controllers/FaqController.php`

page FAQ (`/questions-frequentes` ↔ `/en/faq`) : `faq.categories` dans `lang/{locale}/ui.php` (4 thèmes buying/selling/investing/working, 10 questions chacun, réponses autonomes) → `pages/faq.tsx` = `<PageEyebrow>` + h1 + intro + `components/faq/faq-tabs.tsx` (Figma 696-10782 desktop / 696-10837 mobile : **Radix Tabs** — colonne de thèmes à gauche en desktop (`sticky top-24` sous le header, rangées pleine largeur, mêmes surfaces que l'accordéon : actif = `bg-background-08` + couleur titre, hover = `bg-background-05`, jamais de changement de graisse ni de capitales, compteur de questions `tabular-nums` à droite), sur mobile **le même style et la même taille** (`text-sm`, rangées sable) en bande défilante (snap, `scrollIntoView` du thème actif, fondu à droite — pilules à bordure puis trait dessiné essayés et retirés, décision utilisateur) ; panneau = `ui/accordion.tsx` (variante Figma 278-10243/10244 : bloc `p-6`, fond `bg-background-08` ouvert, hover `bg-background-05`, question `text-base font-semibold` muted → foreground, icône `components/page/accordion-toggle-icon.tsx` plus → moins), 1ʳᵉ question ouverte, h2 `sr-only` par thème car Radix rend les questions en `<h3>`) ; JSON-LD `faqPage()` sur toutes les questions (réponses passées par `stripFaqMarkup`) + breadcrumb ; lien dans le footer. **Au-dessus du design** : recherche client intégrée en tête de la colonne des thèmes (`faq-search.tsx` : encadré `bg-card`, loupe à gauche, chip `kbd` « / » à droite — la touche `/` focalise le champ ; libellé `sr-only` ; question + réponse, sans accents ; ≥ 2 caractères ; résultats à plat dans le panneau avec le thème en surtitre, tous ouverts, thèmes grisés ; aucun → CTA contact ; choisir un thème efface la recherche), **ancres** `#slug-du-theme` / `#slug-de-la-question` (slugs `Str::slug` calculés dans `FaqController`, restaurés au chargement avec scroll, `history.replaceState` en naviguant), questions ouvertes **mémorisées par thème**, bouton « Tout ouvrir / Tout replier » par thème, CTA « Vous n'avez pas trouvé ? » (`faq-cta.tsx` : bande sable `bg-background-08` d'une ligne, `Button variant="outline"` **à fond blanc `bg-card`** (décision utilisateur 2026-09-21 ; avatar du conseiller `seo.advisor.photo` à gauche du libellé, pas d'icône) vers contact — choisi parmi 10 variantes ui.sh) en fin de thème et en état « aucun résultat ». Liens dans les réponses : mini-markup `[libellé](nom.de.route)` dans `ui.php` (`lib/faq-markup.ts` parse/strip, `faq-answer.tsx` rend des `<Link prefetch>` avec `linkClass`) — `FaqTest` vérifie que chaque route citée existe

### `resources/js/components/page/page-backdrop.tsx`

façade haussmannienne en hairlines SVG (toit à la Mansart + lucarnes, double corniche, balcon filant, travées de 192px) derrière l'en-tête de toute page publique (montée par `PublicLayout`, désactivable par la prop `backdrop={false}` — À propos, dont le hero est une photo) : `text-border`, `opacity-20`, fondue par l'utilitaire `backdrop-fade` (`app.css`, masque bas + côtés — les `mask-*` Tailwind n'existent qu'en 4.1), `hidden sm:block`, `aria-hidden`, props `className` et `gradient` (trait en dégradé sable) disponibles ; le manifeste À propos l'a utilisée puis est passé aux **anneaux de la carte CTA** (`RingsBackdrop className="size-[64rem]"`, décision utilisateur 2026-09-16, décision utilisateur 2026-09-16). Décision utilisateur 2026-08-27 : « très léger », filigrane, jamais un motif visible

### `resources/js/components/footer/`

social-links (réseaux depuis config/seo.php `social`), contact-card (avatars des conseillers, téléphone, puis phrase de disponibilité en dessous — décision utilisateur 2026-09-16 ; tout le bloc est un lien vers contact), footer-column, footer-nav (deux colonnes : « Nos services » = acheter / vendre / estimation, « À propos » = contact / newsletter / FAQ / blog / à propos — décision utilisateur 2026-09-16 ; sur desktop la colonne contact reste en dernier, **sur mobile/tablette elle passe en premier** (`order-first lg:order-none`) - décision utilisateur 2026-09-16 ; **les deux colonnes de nav restent côte à côte dès le mobile** (grille `grid-cols-2`, marque et contact en `col-span-2` sous `lg`) - décision utilisateur 2026-09-22), legal-bar (© + liens légaux vers les vraies pages), brand-wordmark (wordmark contour pleine largeur en tête du footer ; **sur mobile / tablette il touche les bords** (`-mx-6 lg:mx-0`) et est un peu plus visible (`opacity-100 lg:opacity-80`) — décision utilisateur 2026-09-22)

### `resources/js/components/navigation/`

nav-link (lien + hover ellipse + focus-ring), nav-divider, nav-items (entrées + useIsActive), menu-toggle-icon (3 traits → croix, 300 ms, aussi rapide que le déploiement du menu), mobile-menu-toggle (bouton 72×48, aria-expanded/controls), mobile-menu-panel (Figma 137-3968 : **voile flouté** sous la barre (`bg-background/30`, `absolute top-full`, hauteur `100dvh − barre`, un tap dessus ferme ; le **flou n'est jamais transitionné** (les navigateurs le rasterisent en retard, tout fondu le fait apparaître en deux temps) : il apparaît d'un coup avec le panneau blanc et la barre (`duration-0`) ; seule la **couche de teinte** (span séparé `bg-background/30`) fond en 200 ms **à l'ouverture** ; la fermeture est instantanée pour tout — voile, teinte, panneau, rangées démontées — aucune cascade en sortie, décision utilisateur 2026-09-16) sur lequel le panneau blanc **ne prend que la hauteur de son contenu** (décision utilisateur 2026-09-16) ; **ouverture en pile qui se déploie** : fondu du panneau + chaque rangée (liens, hairlines, bloc CTA) monte de 12px en fondu, expo-out 350 ms, décalage 35 ms par rangée via `--stagger` (`animate-menu-in`, `app.css`, `motion-reduce` coupe) — décision utilisateur 2026-09-16 ; liens `NavLink plain` en `font-medium` sur le 1er niveau (pas de trait au survol : hover `bg-background-05`, actif `bg-background-08`) précédés d'une icône lucide grise fine (KeyRound / Tag / House, `ICONS` dans mobile-menu-panel) + hairline verticale ; l'entrée Estimation porte à droite un badge sable **à angles droits** « Sous 24 h » (variante ui.sh « Carré sable ») qui passe en blanc `bg-card` au survol / focus / page courante du lien (`nav.estimate_badge`, incitation ; « Gratuite » jugé trop cheap, décision utilisateur) `bg-border h-5 w-px` (variante ui.sh « Hairline verticale », décision utilisateur 2026-09-16), liens secondaires blog / FAQ / à propos (`useSecondaryNavItems`) dans un **puits en dégradé sable** (`from-background-08 to-transparent p-2`, variante ui.sh « Dégradé sable », **hover / focus / page courante en blanc `bg-card`** sur le puits sable — le 1er niveau garde son hover sable, décision utilisateur 2026-09-16), puis CTA + LanguageLinks directement dessous ; fermeture instantanée (rangées rendues seulement quand ouvert, remontées à chaque ouverture via un compteur en `key` pour rejouer le déploiement) ; l'entrée Estimation s'appelle « Estimer mon bien » sur mobile (`NavItem.mobileLabel`, `nav.estimate_mobile`) (décision utilisateur 2026-09-16 ; une version « 52px + icônes + barre sticky avec téléphone + langues en toutes lettres » a été construite puis retirée le même jour), scroll body verrouillé, Échap, swipe haut, fermeture auto ≥ lg, focus sur le 1er lien). État `menuOpen` dans site-header. Fond de la barre : **blanc opaque partout, jamais de blur** (mobile comme desktop, en haut de page comme au scroll — décision utilisateur 2026-09-16) ; la carte flottante devient une barre bord à bord au scroll. Hairline en dégradé sous la barre : **desktop uniquement, au scroll** ; sur mobile la barre n'en a pas, mais le **menu ouvert** commence par une `GradientHairline` entre la barre et la première rangée (décision utilisateur 2026-09-22, remplace « jamais sur mobile » du 2026-09-16)

### `resources/js/components/home/cta-card.tsx`

appel à l'action de fin de page (Figma 712-24182 / 712-24510, affiché sur la HP pour l'instant) — restylé sur décision utilisateur 2026-09-16 (variante ui.sh « Carte sable du site ») : carte `border-secondary-30 bg-card p-2` + dégradé sable, fond décoratif = `components/page/rings-backdrop.tsx` (**partagé avec le manifeste À propos**, id de dégradé unique par instance via `useId`) : 5 cercles concentriques en hairline (SVG `aria-hidden`, trait en dégradé sable `secondary-50 → 30 → transparent`, opacité très basse — une version plus visible a été essayée puis annulée) qui tournent en 60 s (`animate-rings`) et respirent en 10 s (`animate-rings-breathe`), `motion-reduce` — variante ui.sh « Cercles concentriques » adoucie, décision utilisateur 2026-09-16 ; un damier animé puis une mosaïque de carrés gris ont été essayés et rejetés (« pas premium »), 3 avatars conseillers `size-12` (sans pilule « 2 500+ » ni vagues pointillées du Figma), h2 `cta.title`, phrase `cta.text`, bouton plein `cta.button` → contact

### `resources/views/app.blade.php`

layout racine. Pas de <title> statique : il vient de <Head>/@inertiaHead. Google Tag Manager via `<x-analytics.gtm-head/>` (tout en haut du <head>) et `<x-analytics.gtm-body/>` (juste après <body>) — `resources/views/components/analytics/`, rendus uniquement si `services.gtm.id` (`GTM_ID`) est défini : jamais d'ID en dur, vide en local/tests. Conteneur prod : `GTM-NNPNBZTS` (à renseigner dans Laravel Cloud). Test `tests/Feature/GoogleTagManagerTest.php`

### `config/inertia.php`

ssr.enabled ← INERTIA_SSR_ENABLED (.env) ; `ssr.connect_timeout` (1 s) / `ssr.timeout` (5 s) appliqués par `app/Http/Ssr/ResilientHttpGateway.php` (lié à `Inertia\Ssr\Gateway` dans AppServiceProvider) : un serveur SSR injoignable rend côté client en ~1 s au lieu de bloquer la requête 30 s — cause de l'erreur « upstream connect error … connection timeout » vue sur Laravel Cloud le 2026-09-16 avec `INERTIA_SSR_ENABLED=true` sans process SSR

### `resources/js/pages/home.tsx`

**2026-09-22, ordre de conversion (décision utilisateur)** : un seul tunnel attention → action, chaque h2 en question à laquelle le bloc suivant répond, bouton plein seulement dans le hero et la carte finale : 1. `Hero` (promesse + « Contacter un conseiller » + 3 engagements) → 2. `TrustIntro` (chiffre réel, conseillers, note Google, « Découvrir nos biens » / contact) → 3. `Services` « Que pouvons-nous faire pour votre projet immobilier à Paris ? » (auto-segmentation) → 4. `AgencyManifesto` « Qui sommes-nous ? » **en texte seul** (prop `stats` omise : le bloc de confiance porte déjà le chiffre ; les cartes de quartier essayées à cette place ont été retirées) → 5. `SuccessStories` « Quels résultats obtenons-nous pour nos clients ? » **sans la citation ni le bouton** sous le carrousel (`quote={false}`) ; **carrousel fini** : la boucle infinie en trois copies a été retirée, les flèches déplacent la rangée d'une photo et sont **désactivées (atténuées) au début / à la fin de la rangée**, mesurées sur la position de scroll (un index de carte courante ne touchait jamais les dernières photos sur desktop, bug 2026-09-22) ; la dernière photo porte `lg:last:snap-end` pour être vue en entier en fin de rangée (avec `snap-start` seul, la rangée s'arrêtait sur l'avant-dernière et la coupait, bug 2026-09-22) — décision utilisateur 2026-09-22 → 6. `Testimonials` « Que disent nos clients ? » → 7. `FaqTeaser` « Comment travaille-t-on avec Estate in Paris ? » (thème `working`, 6 questions, `home.faq.*`, JSON-LD `faqPage` ajouté au `siteGraph`) → 8. `BlogRelated` « Que faut-il savoir avant d'acheter ou de vendre à Paris ? » (sortie douce) → 9. `CtaCard`. `SharedTranslations` : `home` partage aussi `faq` et `about` (manifeste). Les titres `services.title`, `stories.title`, `testimonials.title` (partagé avec À propos / Acheter / Vendre) passent en question.

## Audit de comportement 2026-09-22 (décision utilisateur : « vérifie chaque composant sur ce genre de détail »)

Deux audits en lecture seule (carrousels / rangées défilantes ; états actifs, scroll, formulaires) puis correction avec test de non-régression pour chaque point.

- Stepper d'estimation : la saisie au clavier passe par un brouillon local pendant le focus (champ vidé = vide, commit + clamp au blur / Entrée, Entrée ne soumet pas le formulaire) ; −/+ en bout de plage = `aria-disabled` + no-op, jamais `disabled` (le focus reste sur le bouton) ; chambres plafonnées par les pièces côté client (`max={rooms}`, baisse des pièces → baisse des chambres dans la même mise à jour).
- Autocomplétion d'adresse : `close()` invalide la requête Places en cours et une réponse n'ouvre la liste que si le champ a encore le focus (plus de liste qui réapparaît après effacement ou Tab).
- `NumberedSteps` (stratégies Acheter, étapes Vendre) suit le scroll comme les valeurs À propos (élément sous la ligne médiane, sinon le plus proche, `rAF` coalescé) : l'`IntersectionObserver` sur une bande de 10 % sautait une étape courte. Les tests mockent `getBoundingClientRect` + `innerHeight`.
- Bandeau d'engagements À propos : pause au `focus-within` et au toucher, plus un bouton Pause / Lecture (`outline icon`, `aria-pressed`, `about.marquee_pause/play`) à droite de la bande, masqué en `motion-reduce` (WCAG 2.2.2).
- FAQ : l'accordéon des résultats se remonte à chaque requête (`key={query}`) et ses ids sont préfixés `search-` ; tout `scrollIntoView` du site passe par `scrollBehavior()` (`lib/focus-field.ts`) : `auto` sous `prefers-reduced-motion`.
- Façades vidéo (blog, hero de service) : le focus passe sur l'iframe une fois le bouton démonté ; barre de lecture à 0 pour un article plus court que l'écran ; menu mobile : `#main` et `#footer` (id ajouté) sont `inert` tant que le menu est ouvert, et la fermeture par passage en desktop ne refocalise le toggle que s'il est affiché.

- Carrousels : « Suivant » se désactive dès que la rangée tient à l'écran (`scrollLeft >= max - 1`, sans garde `max > 0`) mais jamais sans layout mesurable ; `cursor-grab` seulement quand la rangée déborde (`data-overflowing`) ; `current` des flèches services / équipe recalculé au montage et au `resize` ; le dernier item snap sur sa fin en mobile (`max-sm:last:snap-end`, articles liés du blog).
- Témoignages : pas des flèches = distance réelle entre deux cartes (plus de `+ 20` fixe) ; flèches rendues sous la rangée quand `seo.reviews` manque (rangée toujours pilotable au clavier) ; citations sélectionnables hors glisser (`data-[dragging=true]:select-none`).
- Barre mobile « Vendre » : masquée dès que la carte CTA finale est visible **ou déjà passée** (ne revient que si la carte repasse sous le viewport) ; un espaceur `h-20 lg:hidden` réserve sa hauteur sous le contenu.
- Hero À propos : la molette ne boucle plus (au dernier / premier message l'événement passe et la page défile) ; `touch-pan-y` et aucun `preventDefault` tactile (seul un flick ≥ 40 px en < 300 ms change de message, les points restent la voie sûre) ; roving tabindex sur les points (le focus suit ↑/↓) ; `settle()` immédiat sous `prefers-reduced-motion`.
- `useDragScroll` : `dragstart` bloqué pendant le glisser (plus d'image fantôme) ; `data-dragging` retiré après la fin du scroll (`scrollend`, sinon 400 ms) pour ne pas couper le glissement vers l'item suivant ; test `tests/js/hooks/use-drag-scroll.test.tsx`.

## Historique des choix (chronologie)

- `@inertiajs/react` épinglé en `^2` (2.3.x) : la v3 casse `createInertiaApp`, la 2.0.3 d'origine avait un typage `useForm` trop strict.
- La page démo `welcome.tsx` du kit a été remplacée par `pages/home.tsx` (PublicLayout `hero`, textes `home.*`, route `/` → `HomeController`). **HP (2026-09-16) = hero + intro de confiance (2026-09-22) + services + témoignages + success stories + aperçu du blog (2026-09-22) + carte CTA.** **Aperçu du blog** (décision utilisateur 2026-09-22) : `BlogRelated` réutilisé avec ses props `header` (eyebrow `blog.title`, h2 `home.blog_title`, intro GEO `home.blog_intro`, centré) et `cta` (`home.blog_cta` → `blog.index`, bouton outline) ; prop `posts` = 3 derniers articles de la langue via `ListBlogPosts` dans `HomeController`, **`rescue()`** : une panne Sanity est reportée mais ne fait jamais tomber la home (bloc masqué, test `HomeTest::test_home_hides_the_blog_preview_when_sanity_fails`) ; `HomeTest` fake Sanity (`Http::fake`, à refaire après `withLocale('en')` qui recrée l'app). **Carte CTA** (`cta-card.tsx`) ajoutée en fin de page sur Vendre, Estimation, FAQ et l'index du blog (`max-w-5xl`, décision utilisateur 2026-09-22) : elle est désormais sur toutes les pages publiques sauf contact, newsletter et les articles. **Intro de confiance** `components/home/trust-intro.tsx` (Figma 712-25112 desktop / 712-25584 mobile, décision utilisateur 2026-09-22) juste sous le hero : à gauche h2 Montserrat sur deux lignes (`text-2xl sm:text-3xl lg:text-4xl`) = **chiffre réel** en semibold (prop `figure` = `about.stats.2.value` « 500+ » via `HomeController` — le « 2,500+ » du Figma n'a pas de source) + `home.trust_title_1` puis `trust_title_2`, ligne de preuve = 3 portraits des conseillers `size-8` + **logo Google** (`public/images/social/google.svg` en `<img>`, « Google » en sr-only — l'étoile du Figma et le mot « Google » retirés, décision utilisateur 2026-09-22) + note `seo.reviews.rating` + `testimonials.based_on` (masquée sans chiffres) ; à droite phrase GEO `home.trust_text` + deux boutons (`trust_cta_buy` plein → buy, `trust_cta_contact` outline → contact) ; centré et empilé sur mobile avec boutons pleine largeur, deux colonnes dès `lg` ; tests `trust-intro.test.tsx` + `HomeTest`. **Success stories** `components/home/success-stories.tsx` (Figma 712-25407 / 712-25844, décision utilisateur 2026-09-16) : cartes photo à angles droits avec voile noir en bas et texte par-dessus (eyebrow « Success story », titre Montserrat, lieu · délai · résultat avec icônes MapPin / CalendarDays / TrendingUp), la 1ʳᵉ carte sur deux tiers en desktop (`lg:grid-cols-3` + `col-span-2`), **une carte centrée à la fois** + flèches sur mobile / tablette (même mécanique que services / équipe) ; dessous citation Montserrat centrée `stories.quote` **écrite mot à mot à l'arrivée dans le viewport** (même `animate-manifesto-in` + `--stagger` 40 ms/mot que le manifeste À propos, bouton 100 ms après le dernier mot) et bouton `stories.cta` → **blog** (pas encore de page dédiée ; les cartes ne sont pas des liens) ; données `stories.items` (`ui.php`, prop `stories` du contrôleur, photo avec `{w}` pour le srcset) — **chiffres à remplacer par de vrais dossiers**, photos `public/images/stories/story-{1,2}-{800,1600}.jpg` = placeholders du Figma. **Services** `components/home/services.tsx` (Figma 712-25176 / 712-25611, adapté à la DA, décision utilisateur 2026-09-16) : en-tête centré `services.*` (eyebrow, h2, intro GEO), 4 cartes-liens (acheter / vendre / estimation / **accompagnement → contact**, la carte « Relocation » du Figma n'ayant pas de page ici) = **carte du site** (`border-secondary-30 bg-card p-2` + dégradé sable intérieur, décision utilisateur 2026-09-16), titre Montserrat `text-lg font-medium` (les capitales du Figma, jugées trop lourdes, retirées), phrase, icône lucide (KeyRound / Tag / ChartNoAxesCombined / UsersRound) dans un carré `size-11` en bas ; au survol / focus : hairline `secondary-50`, carré d'icône `bg-primary`, grand filigrane sable de l'icône dans le coin (visible en permanence à 25 %, renforcé au survol : sinon jamais vu sur mobile) — **pas de cadre pointillé** (rejeté), pas d'ombre ; mobile / tablette = carrousel `snap-x` glissable à la souris, **chaque carte verrouillée au centre, la première comprise** (`snap-center`, marges latérales `calc(50% - demi-carte)`, `useDragScroll({ align: 'center', open: 'first' })` : l'option `open: 'first'` laisse la rangée à son début au lieu de l'ouvrir sur l'item du milieu) avec **seulement des flèches précédent / suivant** dessous (`Button outline icon`, désactivées en bout, même retour à l'appui que les témoignages ; les points du Figma ont été construits puis retirés — décisions utilisateur 2026-09-16), grille 4 colonnes dès `lg` ; test `tests/js/components/services.test.tsx`. Hero `components/home/hero.tsx` (Figma 712-26754) : plein écran `min-h-svh`, photo pleine largeur qui passe **derrière le header** (`main` sans padding ni max-width, `-mt-16 lg:-mt-19`, `SiteHeader overlay` garde transparente la bande au-dessus de la carte) — `public/images/home/hero-{800,1200,2000,2800}.jpg` dérivés de la photo « façade haussmannienne » 4096px du Figma (node 728:32) ; voile 30 % + dégradé vertical `via-black/45` derrière le bloc central + dégradé noir en bas ; révélation au chargement (photo `animate-hero-photo` zoom 1.05 → 1, puis eyebrow / h1 / phrase / bouton / rangée `animate-hero-rise` en cascade via `--stagger`, `motion-reduce` annule) ; bloc centré = eyebrow capitales `home.hero_eyebrow` encadré de deux traits blancs, h1 Montserrat bold 2 lignes `hero_title_1/2`, phrase GEO courte `hero_text`, bouton `neutral` `hero_cta` → contact, textes blancs avec `drop-shadow` ; rangée des 3 engagements `hero_value_1..3` (Building2 / Navigation / MessageCircleQuestion, hairlines en dégradé verticales (même `via-border` que partout ; une teinte sable a été essayée puis retirée, décision utilisateur 2026-09-16) en dégradé blanc) sur une ligne à toutes les largeurs — sur mobile **carrousel à accroche** (`snap-x snap-mandatory`, items `snap-center`, `px-[40vw]`, ouvert sur l'item du milieu, glissable à la souris via `hooks/use-drag-scroll.ts`). **Témoignages** `components/home/testimonials.tsx` (Figma 712-25299 / 712-25738, structure conservée et retravaillée dans le style du site — décision utilisateur 2026-09-16 après revue de 20 variantes : bande `from-background-05 to-background`, en-tête centré `testimonials.*`, colonne note = `seo.reviews` (variante ui.sh « Note géante », décision utilisateur 2026-09-16 : note `text-5xl sm:text-7xl` `/5` (réduite sur mobile, décision utilisateur 2026-09-22), puis « Basé sur N avis » (les étoiles sable sous la note ont été **retirées** le 2026-09-16, décision utilisateur ; elles restent sur les cartes) précédé du **logo Google** (`public/images/social/google.svg`, logo de marque en `<img>`, « Google » en sr-only) en lien vers `reviews.url`, puis **portraits ronds** superposés + tuile ronde `+N` **en bas de la colonne** (`justify-between` : les portraits se collent dessous essayé puis annulé, décision utilisateur 2026-09-16) face aux **flèches précédent / suivant** `Button outline icon` qui font défiler la rangée d'une carte ; **défilement automatique** (décision utilisateur 2026-09-16 : une carte toutes les 5 s via le même `scrollBy`, **sans bouton Pause / Lecture** (retiré à la demande de l'utilisateur), en pause au **survol des cartes** / focus dans la rangée / toucher / onglet caché / bloc hors viewport (IntersectionObserver), jamais sous `prefers-reduced-motion`, délai relancé après chaque flèche) (retour à l'appui : bouton `active:scale-90`, chevron qui glisse de 2px dans le sens du défilement via `group-active`, `motion-reduce`) — logo EIP et bouton « Laisser un avis » retirés ; **masquée sans chiffres réels**), `GradientHairline vertical` entre la colonne et les cartes, rangée de cartes-citations = **10 avis** (`testimonials.items` ; les 7 derniers n'ont pas de photo : initiales en repli, portraits à fournir avec l'accord des clients) en **boucle infinie** (décision utilisateur 2026-09-16 : la rangée est rendue trois fois, on reste dans la copie du milieu — recalage instantané d'une largeur de copie sur `scroll`, et avant un défilement fléché qui franchirait le seuil ; copies hors milieu en `aria-hidden` ; `useDragScroll` calcule le glissement par deltas incrémentaux pour survivre au recalage) — carte du site (`border-secondary-30 bg-card p-2` + dégradé sable, angles droits, sans ombre, `Quote` sable, étoiles, citation Montserrat `text-lg/7`, portrait rond + nom + contexte) en `snap-x` horizontal à toutes les largeurs avec fondu à droite **en desktop uniquement** (retiré sous `lg`, décision utilisateur 2026-09-22), **glissable à la souris** (`useDragScroll`, `align: 'start'` dès `lg` ; **sous `lg` une carte est toujours centrée** : `snap-center`, marges `px-[calc(50%-10rem)]`, `align: 'center'` via `matchMedia` — décision utilisateur 2026-09-16) ; sur mobile la note ouvre le bloc et portraits / étoiles / bouton le ferment, comme le Figma ; textes `testimonials.items` dans `ui.php`, portraits `public/images/testimonials/client-{1,2,3}.jpg` = **placeholders exportés du Figma, à remplacer par de vrais clients avec leur accord**). Carte CTA : voir `components/home/cta-card.tsx`. La bande de confiance (2 500+ propriétaires, note Google, 4 cartes services), le marquee de logos et le carrousel de biens ont été construits puis **supprimés le 2026-09-16** (décision utilisateur ; les vrais biens viendront d'un catalogue).
- Le callback `title:` de `createInertiaApp` a été retiré (app.tsx + ssr.tsx) : le suffixe est géré par `<SeoHead>` pour éviter « Titre - Laravel - Laravel ».
- Fonts : Instrument Sans (kit) → Inter + Montserrat self-hosted (décision utilisateur, perf), d'abord via fontsource puis en `@font-face` maison latin-only + preload.
- Header : 64px (compact 56px au scroll) ; le mega-menu « Nos biens » (mega-menu-column/-promo/-properties) a été construit puis **supprimé** (jamais branché, décision utilisateur) ; le header « overlay » sur le hero a été retiré puis **remis le 2026-09-16** (décision utilisateur, Figma 712-26754). Header mobile : se cache au scroll bas / revient au scroll haut ; menu sous la barre (header conservé, icône → croix) avec swipe vers le haut pour fermer et fermeture auto au passage en desktop.
- Tests : PHPUnit classique, pas Pest (le kit n'installe pas Pest).
- `ui/dropdown-menu.tsx` et `ui/button.tsx` mis à jour vers le shadcn actuel (new-york / Tailwind v4 : `default h-9 px-4`, `sm h-8`, `lg h-10 px-6`, `icon size-9`, `data-slot`, icônes `size-4` par défaut sauf classe `size-*` explicite). Les autres composants `ui/` sont encore ceux du kit (style « default », Tailwind v3) : les mettre à jour un par un avec `--overwrite` quand on y touche, en vérifiant les usages du kit (auth, settings, sidebar).
- Git initialisé le 2026-08-23, remote `origin` = https://github.com/benjaminroche4/eip (branche `main`).
- **2026-09-22** : politique de confidentialité rédigée (adaptée de celle de Relocation in Paris aux traitements réels du site : contact, estimation, newsletter ; sous-traitants Laravel Cloud / Resend / Google Tag Manager / Google Places ; responsable Veylam OÜ ; test `LegalPagesTest::test_privacy_policy_is_real_and_names_the_actual_processors`). **Page « Conditions générales » supprimée** (route, slug, textes, sitemap) et **remplacée par un plan du site HTML** (`SitemapPageController`, `pages/sitemap.tsx`, `/plan-du-site` ↔ `/en/sitemap`, lien dans la barre légale du footer à la place des CGU ; **catégories et articles du blog listés aussi**, décision utilisateur 2026-09-22) — décision utilisateur.
- **2026-09-22** : plan du site, 16 variantes ui.sh comparées (trois colonnes, numéros sable, cartes, sommaire sticky, liste descriptive, chips, arborescence, bandeau sable, éditorial, quatre colonnes, accordéon, chemins d'URL, puits sable, compteurs, pages | blog, icônes) → **« Éditorial »** retenue, titres ramenés à la taille des h2 des mentions légales (`text-xl font-medium`) — décision utilisateur.
- **2026-09-22** : plan du site, revue SEO / UX (« fait tout ») : `ItemList` JSON-LD de tous les liens, articles groupés par catégorie (h3 → page catégorie, « Autres articles » pour les orphelins), Accueil en tête d'un groupe « Pages du site », lien Recherche retiré (page placeholder), page citée dans `llms.txt`, pastille de compte à côté de chaque h2.
- **2026-09-22** : espacements verticaux resserrés sous `lg` — `PublicLayout` haut `pt-10` mobile / `sm:pt-12` tablette (80px gardés en desktop), marges négatives dépendantes ajustées (fil d'Ariane des articles, hero À propos) ; footer mobile : wordmark `pt-8`, grille `gap-8 py-8`. Décision utilisateur (« trop d'espace entre la barre et les en-têtes », idem entre wordmark, contact et logo du footer).
- **2026-09-22** : prop Inertia `translations` découpée par route (`SharedTranslations`, garde `SharedTranslationsTest`) : la page FAQ passe de ~91 Ko de props à la seule section `faq` + layout ; recherche FAQ partageable via `?q=`. Décision utilisateur après le check-up de la page FAQ.
- **2026-09-22** : l'aperçu des derniers articles ajouté sur la page newsletter a été **retiré** le jour même (décision utilisateur : « remets-la comme avant ») ; la page garde son intro GEO et la carte d'inscription. L'aperçu du blog reste sur la home.
- **2026-09-22** : carte newsletter — la ligne « Note 4,9/5 sur Google » ajoutée le jour même est **retirée**, la pilule « Pas de spam » est **centrée** seule sous le formulaire (décision utilisateur).
- **2026-09-22** : bloc « avantages » de la page newsletter — le h2 visible en question et les tuiles d'icône carrées essayés le jour même sont **annulés** : titre de nouveau en `sr-only`, icônes dans des disques `rounded-full` comme avant (décision utilisateur : « je préfère rond en vrai »).
- **2026-09-22** : menu mobile — la hairline sous la barre (ajoutée le jour même) est **retirée**, et la pile est resserrée : `gap-3` entre les rangées et le puits secondaire (`mt-2`), bloc bouton « Nous contacter » + liens de langue à `mt-3 gap-3` (décision utilisateur).
- **2026-09-22** : page Vendre — le bloc quartiers (« Combien vaut le m² dans votre quartier ? », cartes `DistrictCards`) est **retiré** et remplacé par un **bloc texte + photo en deux colonnes** `components/page/feature-split.tsx` (générique : eyebrow, h2 en question, intro GEO, liste à coches, photo nue **carrée** (3/2 puis 4/5 essayés, décision utilisateur 2026-09-22), prop `reverse`) sur la **vente confidentielle** (`sell.confidential.*`, photo placeholder `stories/story-2`) — décision utilisateur.
- **2026-09-22** : bloc témoignages — sous la note géante, « Basé sur 400 avis » devient **« +400 avis »** (clé `testimonials.count_short` ; `based_on` reste pour l'intro de confiance et le hero À propos) — décision utilisateur.
- **2026-09-22** : success stories (home) — **photos seules** : le voile noir et le texte (eyebrow, titre, lieu · délai · résultat) sont retirés, l'histoire reste dans l'`alt` ; **une seule rangée `snap-x` à toutes les largeurs** (la grille 2/3 + 1/3 desktop disparaît), photos au **format des anciennes cartes** (`w-80 sm:w-96 lg:w-104`, `h-96 sm:h-112 lg:h-120`), **6 photos** (`stories.items` : 4 entrées ajoutées, photos `story-3..6` = copies des placeholders Acheter, textes non affichés à remplacer par de vrais dossiers), glissables souris / doigt, **rangée calée à gauche dès `lg`** (`lg:px-0`, `lg:snap-start`, `useDragScroll align: 'start'` via `matchMedia` comme les témoignages ; la première photo centrée avec un demi-écran vide « commençait au milieu », décision utilisateur 2026-09-22), une photo centrée sous `lg`, **flèches précédent / suivant visibles partout** ; citation et bouton blog conservés — décision utilisateur.
- **2026-09-22** : page Vendre — la mosaïque de photos (`sell-gallery`, 3 photos) est **remplacée par la rangée de photos de la home** (`SuccessStories` : photos seules, glissable, flèches ; sans la citation ni le bouton blog, `quote={false}`, `embedded`) — décision utilisateur. Composant, test, clés `sell.gallery` et fichiers `gallery-*` supprimés.
- **2026-09-22** : « Notre bilan » (Acheter) répétait les 4 chiffres clés du hero → remplacés par **4 faits réels distincts** (note Google + nombre d'avis depuis `seo.reviews`, réponse sous 24 h ouvrées, conseil FR · EN, agence Paris 6e rue Grégoire de Tours), la ligne de preuve devient « Vos conseillers dédiés ». Prop `facts` (`BuyController::facts()`), plus de compteur sur ce bloc.
- **2026-09-22** : `useDragScroll` — au relâchement d'un glisser à la souris, un déplacement de plus de 24px avance toujours d'au moins un item dans le sens du geste (avant : accroche sur l'item le plus proche, donc retour en arrière sur un glisser court ; « l'image revient en arrière », décision utilisateur).
- **2026-09-22** : footer « Nos services » — entrée **Relocation** vers https://relocation-in-paris.fr/ (agence sœur ; URL dans `config/seo.php` `relocation_url` ← `SEO_RELOCATION_URL`, partagée en `seo.relocationUrl`, lien externe nouvel onglet) — décision utilisateur.
- **2026-09-22** : rangée de photos (success stories, home + Vendre) en **boucle infinie** : même mécanique que les témoignages (trois copies, recalage invisible d'une largeur de copie au scroll et avant un défilement fléché, copies extérieures `aria-hidden`), flèches jamais désactivées — décision utilisateur.
- **2026-09-22** : nouvelle page **« Arrondissements de Paris »** (carte SVG interactive + fiche au survol + tableau des 20 arrondissements : profil idéal, prix moyen au m², atout) — décision utilisateur. Géométrie générée depuis l'open data (`https://opendata.paris.fr/api/explore/v2.1/catalog/datasets/arrondissements/exports/geojson`) par un script Python (projection équirectangulaire centrée sur Paris, viewBox 1000 px, Douglas-Peucker 0,8 px, centroïdes `geom_x_y`) → `resources/js/lib/paris-arrondissements.ts`. Prix : mêmes arrondis que les cartes quartiers d'Acheter (6e 14 500, 7e 14 000, 8e 12 500, 16e 11 000), les 16 autres estimés depuis les publications des Notaires du Grand Paris 2026 — **à vérifier avant mise en ligne**.
- **2026-09-22** : carte des arrondissements enrichie (décision utilisateur « fais tout sauf le 4 ») : dégradé de prix + légende, relief au survol (actif sombre et soulevé, voisins atténués), infobulle suivant le curseur en desktop, révélation en cascade, Seine (dérivée des frontières partagées rive droite / rive gauche, Overpass injoignable) et bois hachurés (dataset `espaces_verts`). Les filtres par profil (4) sont laissés de côté.
- **2026-09-22** : page Arrondissements restructurée (décision utilisateur) : carte centrée, **survol ≠ sélection** (le survol allume la carte et l'infobulle, le clic / tap / clavier sélectionne), la **fiche détaillée s'affiche sous la carte** à la place du tableau (points forts, à qui ça s'adresse, typologie de logements, prix au m², profil idéal) ; les 20 fiches restent dans le HTML (`hidden`) pour le SEO. Données enrichies (`positives`, `audience`, `housing`) en FR et EN.
- **2026-09-22** : carte des arrondissements, seconde passe esthétique (« fais tout ») : contour de Paris papier découpé (silhouette extérieure calculée depuis les arêtes n'appartenant qu'à un seul arrondissement, sommets arrondis à 1e-5°), numéros dimensionnés par surface, Seine sur berge sable, 4 repères (tour Eiffel, Louvre, Notre-Dame, Sacré-Cœur), anneau à la sélection, sélection conservée au survol d'un autre, légende « Plus abordable / Plus recherché », puits sable autour de la carte.
- **2026-09-22** : le puits sable de la carte des arrondissements est animé par les anneaux concentriques du site (`RingsBackdrop`, comme la carte CTA), pas de nouveau mouvement — décision utilisateur.
- **2026-09-22** : carte des arrondissements « un peu en 3D » : plan incliné de 12° (6° au survol) avec épaisseur simulée (silhouette sombre décalée sous une base blanche), infobulle calée sur le puits non transformé — décision utilisateur.
- **2026-09-22** : carte des arrondissements — l'anneau lumineux émis au clic est **retiré** (décision utilisateur).
- **2026-09-22** : puits de la carte des arrondissements — l'aplat sable devient une **photo de Paris floutée et désaturée sous un voile sable** (façade du hero en 800 px, filtre CSS statique, jamais de transition sur le flou), anneaux par-dessus — décision utilisateur (« un effet de ville sous blur »).
- **2026-09-22** : carte des arrondissements — au survol, l'arrondissement **lévite** (monte de 8px, grossit de 4 %, et une copie sable foncé laissée en place révèle son épaisseur), pas d'ombre — décision utilisateur.
- **2026-09-22** : lévitation de la carte affinée — l'arrondissement actif est peint en dernier (plus de collision avec les bords des voisins) et son numéro monte avec lui en semibold — décision utilisateur.
- **2026-09-22** : couleur du survol de la carte des arrondissements : bleu nuit rejeté, remplacé par le **sable profond** (`secondary-80`, numéro sombre), épaisseur en bleu nuit translucide — décision utilisateur.
- **2026-09-22** : carte des arrondissements — les 4 repères deviennent des **monuments stylisés à deux faces** (`landmark-glyph.tsx`, silhouettes simplifiées dessinées à la main : tour Eiffel, pyramide du Louvre, Notre-Dame, Sacré-Cœur), exception documentée à la règle « lucide uniquement » (ce sont des illustrations, pas des icônes) — décision utilisateur.
- **2026-09-22** : monuments de la carte détaillés d'un cran (arches et treillis de la tour Eiffel, quadrillage de la pyramide, rosace et portails de Notre-Dame, dôme nervuré et arcades du Sacré-Cœur) — option 3 de la revue « SVG plus réalistes » retenue par l'utilisateur (« ok ») : seule la tour Eiffel existe en silhouette CC0 sur Wikimedia Commons, les autres monuments n'ont que des fichiers CC BY-SA ou rien ; de vrais fichiers sous licence commerciale pourront remplacer les pictogrammes au même endroit.
- **2026-09-22** : carte des arrondissements — le numéro du 12e est centré sur sa partie urbaine (centroïde du polygone coupé à l'ouest du bois de Vincennes, calculé dans le script de génération) — décision utilisateur.
- **2026-09-22** : carte des arrondissements — les monuments (points et pictogrammes) et leurs libellés sont **supprimés** ; les **numéros passent en léger relief** (copie sable profond décalée dessous) — décisions utilisateur.
- **2026-09-22** : relief des numéros de la carte corrigé — le décalage diagonal donnait un effet de flou ; copie décalée droit vers le bas (3 px) et numéro de devant liseré d'un fin trait (`paint-order: stroke`) pour rester net — décision utilisateur.
- **2026-09-22** : relief 3D des numéros de la carte **retiré** (décision utilisateur), numéros plats.
- **2026-09-23** : page Arrondissements — en-tête remplacé par le hero partagé de Vendre / Acheter (`ServiceHero` : titre + intro + bouton contact) **avec la carte 3D dans l'emplacement de la photo** (prop `panel`, pas de chiffres ni de vidéo ici), la fiche dessous. Quatre dispositions ui.sh (carte puis fiche, côte à côte, bandeau sable, en-tête à gauche) ont été comparées avant cette décision utilisateur.
- **2026-09-23** : fiche d'arrondissement en deux colonnes — texte à gauche en quatre rangées à icônes (points forts, à qui ça s'adresse, typologie, à retenir), carte sticky à droite (prix au m², profil, bouton contact) — décision utilisateur (« la card à droite et le texte à gauche avec des icônes »).
- **2026-09-23** : carte des arrondissements — zoom sur mobile (3 niveaux, boutons + / − et pincement, plan à plat et scrollable une fois zoomé) pour pouvoir sélectionner les petits arrondissements du centre au doigt — décision utilisateur.
- **2026-09-23** : carte des arrondissements — démonstration au chargement (le 6e se soulève seul une seconde après la cascade, une fois, coupée en `prefers-reduced-motion` et par toute interaction) pour signaler que la carte est cliquable — décision utilisateur (option 4 de la revue).
- **2026-09-23** : vidéo de fond du hero de la home (`hero-video.tsx`) depuis `~/Desktop/Vidéo MPEG-4.mp4` (1276×720, 24 fps, 10 s, 6,6 Mo avec audio). Encodages ffmpeg 8 (piste audio retirée, `-movflags +faststart`, 24 fps, 10 s) :
  - `hero-1280.mp4` : `-vf "scale=1280:-2,crop=1280:720" -c:v libx264 -preset slow -crf 19 -profile:v high -level 4.0 -pix_fmt yuv420p` (CRF 22 → 19 le même jour, « légèrement meilleure qualité », décision utilisateur)
  - `hero-1280.webm` : même filtre, `-c:v libvpx-vp9 -b:v 0 -crf 27 -row-mt 1 -deadline good -cpu-used 1`
  - `hero-720.mp4` / `.webm` : `scale=720:-2,crop=720:404`, CRF 21 (H.264, profil main 3.1) / 29 (VP9)
  - `-an` sur les quatre : aucune piste audio (le `muted` du lecteur est une seconde sécurité)
  Choix performance : WebM d'abord, taille mobile séparée, `preload="auto"`, mise en pause sur le poster en reduced-motion / Data Saver / 2G. Le montage client après hydratation avec fondu depuis la photo a été essayé puis **rejeté** le même jour (« c'est la vidéo qui doit s'afficher en premier ») : la vidéo est dans le HTML SSR avec la photo en `poster`. Choix SEO : décorative (`aria-hidden`), aucun balisage vidéo, HTML SSR inchangé — décision utilisateur.
- **2026-09-23** : carrousel de photos (home + Vendre) — les 10 photos du propriétaire (`~/Desktop/1..10.png`, 1360×2048) remplacent les placeholders, converties en **WebP** 480 et 960 px (`cwebp -q 82 -m 6 -sharp_yuv`, 18 à 272 Ko l'unité contre 4 Mo en PNG, 1,7 Mo au total), ordre de rétention : 2, 6, 10, 7, 4, 3, 9, 5, 8, 1 (ouverture sur le salon haussmannien avec vue, alternance pièces habitées / volumes vides, respiration balcon au 4e rang, cuisine en fin) — décision utilisateur.
- **2026-09-23** : carrousel de photos (home + Vendre) en **défilement automatique** d'une photo toutes les 4 s (5 s jugé un peu long), en pause sous la souris (et focus / toucher / onglet caché / hors viewport / reduced-motion), comme les témoignages — décision utilisateur.
- **2026-09-23** : cartes quartiers d'Acheter — les 4 photos du propriétaire (`~/Desktop/1..4.png`, 2048×1360) remplacent les placeholders, en WebP 800 / 1600 px ; correspondance 1 → 6e (Saint-Germain), 2 → 7e (Champ-de-Mars), 3 → 16e (rue arborée), 4 → 8e par élimination (place des Pyramides, rue de Rivoli : scène du 1er, signalée à l'utilisateur).
- **2026-09-23** : témoignages — plus de portraits clients (placeholders), un disque d'initiales en capitales (prénom + nom : « Sophie M. » → « SM ») sur les cartes et dans la colonne de note — décision utilisateur.
- **2026-09-23** : colonne de note des témoignages — les 3 avatars du propriétaire (`~/Desktop/Image PNG*.png`, 2048², convertis en WebP 160 px carré, ~4 Ko) remplacent les disques d'initiales dans la pile « +N » ; les cartes de citations gardent les initiales — décision utilisateur.
- **2026-09-23** : pile d'avatars de la colonne de note — l'homme au milieu : `client-2.webp` (Thomas L., 2e disque) porte désormais le portrait masculin, `client-3.webp` (Claire R.) la blonde ; fichiers échangés, aucun changement de code — décision utilisateur.
- **2026-09-23** : hero « Acheter » — le clip du propriétaire (`~/Desktop/Vidéo MPEG-4.mp4` du jour, séjour parisien lumineux, 1276×720, 24 fps, 10 s) joue **à la place de la photo de façade** dans le panneau (voile, bouton lecture YouTube et carte verre des chiffres restent par-dessus). Composant partagé `components/page/background-video.tsx` (`{ base, poster }`, extrait de `hero-video.tsx` qui devient un wrapper home) + prop `backgroundVideo` de `ServiceHero`. Mêmes encodages ffmpeg que la home (`public/videos/buy/hero-{1280,720}.{webm,mp4}` : 1,58 / 0,71 / 0,33 / 0,22 Mo), poster = 1ʳᵉ image `public/images/buy/hero-poster-1280.jpg` (106 Ko), `buy.photo_alt` décrit désormais le séjour et reste dans le HTML en `sr-only` — décision utilisateur.
- **2026-09-23** : hero « Vendre » — même traitement qu'Acheter : le second clip du propriétaire (`~/Desktop/Vidéo MPEG-4.mp4` de 15 h 55, salon avec vue sur la tour Eiffel) joue à la place de la photo via `backgroundVideo` (`public/videos/sell/hero-{1280,720}.{webm,mp4}`, poster `public/images/sell/hero-poster-1280.jpg`, alt `sell.photo_alt` inchangé : il décrit déjà ce salon) — décision utilisateur.
- **2026-09-23** : hero « À propos » — le troisième clip du propriétaire (`~/Desktop/Vidéo MPEG-4 2.mp4`, **portrait** 856×1072, façade haussmannienne en contre-plongée, 24 fps, 10 s) remplace la photo 4/5 : `BackgroundVideo` reçoit une prop `widths` (`[640, 856]` ici, `[720, 1280]` par défaut) car le clip n'est pas 16/9 ; encodages `public/videos/about/hero-856` (desktop, H.264 **CRF 22** : à 19 la façade très détaillée pesait 3,4 Mo, au-dessus du plafond de 3 Mo des tests / VP9 CRF 27) et `hero-640` (mobile, CRF 21 / 29), poster `public/images/about/hero-poster-856.jpg` ; sur mobile le panneau reste en 16/9 (`object-cover` recadre) ; l'alt `about.hero_photo_alt` (façade haussmannienne) reste vrai et passe en `sr-only` — décision utilisateur.
- **2026-09-23** : cartes d'avis (témoignages home / Acheter / Vendre / À propos) — **logo Google en haut à droite** de chaque carte, sur la ligne du guillemet sable (`justify-between`), pour dire d'où vient l'avis (`public/images/social/google.svg` en `<img alt="">` 20px + libellé `testimonials.source` « Avis Google » en `sr-only` et `title`) ; Trustpilot écarté : les avis viennent de Google — décision utilisateur.
- **2026-09-23** : source des avis — **Trustpilot en plus de Google** : chaque avis `testimonials.items[].source` (`google` par défaut, `trustpilot` sur Julien B., Isabelle D., Anne-Laure P., Nicolas H.), validé dans le DTO `Testimonial` (`SOURCES`, exception sinon), exposé dans la prop ; la carte affiche le logo correspondant en haut à droite (`public/images/social/{google,trustpilot}.svg`, logos de marque en `<img>` ; l'étoile Trustpilot est dessinée dans le vert officiel `#00B67A`) avec `testimonials.source_google` / `source_trustpilot` en sr-only — décision utilisateur.
- **2026-09-23** : logos de source des avis **plus petits** (16 px de haut : Google `size-4`) et **Trustpilot en nom complet** (`public/images/social/trustpilot-wordmark.svg` = étoile verte + « Trustpilot », `h-4 w-auto` ; l'étoile seule `trustpilot.svg` reste disponible) — décision utilisateur.
- **2026-09-25** : ligne de source des prix (`district-cards.tsx`, Acheter / Vendre) — **icône `Info` lucide** `size-3.5` devant le texte (`aria-hidden`, `flex items-start gap-2`) — décision utilisateur.
- **2026-09-25** : section « quartiers » (`district-cards.tsx`) — **plus de padding bas** (`pt-16 sm:pt-20` seul) : le dégradé sable atteint le blanc à 60 %, donc le `pb-16/20` s'ajoutait au `gap-20 sm:gap-28` de la page et l'espace sous la section paraissait plus grand qu'ailleurs — bug relevé par l'utilisateur.
- **2026-09-25** : « Notre bilan » (`buy-record.tsx`) — chaque fait porte une **`key`** (`rating` / `reply` / `languages` / `address`, `ui.buy.record.facts`, type `BuyFact`) au lieu d'un index (le filtre sans note Google décalait les icônes) ; la tuile **Note Google** montre le **logo Google** (`public/images/social/google.svg` en `<img alt="">` `size-7`) à la place de l'étoile ; la tuile **Conseil bilingue** affiche **deux drapeaux** FR + GB (`CountryFlag` `h-6 w-9`) à la place de « FR · EN », qui reste en `sr-only` — décisions utilisateur.
- **2026-09-25** : barre mobile du formulaire d'estimation (`estimate-mobile-bar.tsx`) — **un seul chevron qui se retourne** (`rotate-x-180`, `transition-transform` 300 ms expo-out, `motion-reduce:transition-none`) à l'ouverture / fermeture du récapitulatif, au lieu d'échanger ChevronUp / ChevronDown — décision utilisateur.
- **2026-09-25** : « Qui sont nos conseillers ? » (`team-grid.tsx`, page À propos) — **défilement automatique sur mobile / tablette comme les avis** : une carte toutes les 5 s (`AUTOPLAY_MS`), retour à la première après la dernière (modulo, pas de copies), pause au survol (pointeur non tactile), au toucher, au focus dans le bloc, onglet caché, bloc hors écran (`IntersectionObserver` 20 %), jamais en `prefers-reduced-motion` ni dès `lg` (grille) ; chaque flèche relance le délai ; carte courante suivie par `currentRef` pour l'intervalle — décision utilisateur ; tests `team-grid.test.tsx` (autoplay, survol, flèche, reduced-motion, membre unique).
- **2026-09-25** : carte des arrondissements (`paris-map.tsx`) — **tap mobile** : le toucher ne déclenche plus l'état survolé (`pointerType === 'touch'` ignoré sur `pointerenter` / `pointerleave`, focus donné par un tap ignoré via `lastPointer`) : sur iOS le premier tap émule un survol et, comme ce survol changeait le DOM (pièce soulevée), le clic était avalé — impossible de sélectionner un second arrondissement (bug utilisateur) ; **pincement** : `touch-pan-y` sur le plan dès 100 % (la page défile toujours, mais deux doigts nous reviennent au lieu de déclencher le zoom navigateur qui annulait les pointer events), `touch-pan-x` ajouté une fois zoomé ; **pièce survolée / sélectionnée au-dessus de la Seine**, des bois et du contour : l'overlay de lévitation est le dernier enfant du SVG — décisions utilisateur ; tests `paris-map.test.tsx` (pincement, taps successifs, ordre de l'overlay).
- **2026-09-25** : légende de la carte des arrondissements (`paris-map.tsx`) — variante ui.sh **« Surtitre »** choisie parmi 21 (barre centrée, règle pleine largeur, curseur et prix, graduations, chip prix, hairlines, deux colonnes, prix min / max, nuancier, bandeau sable, chiffre en tête, perle, pilules, liste de définitions, pourcentage, numéros aux extrémités, barre segmentée, consigne en premier, chevron, éditorial écartés) : eyebrow « Prix moyen au m² » au-dessus de la jauge, libellés **« Prix le plus bas » / « Prix le plus élevé »** (« Plus recherché » jugé vague), remplissage de la jauge en 700 ms expo-out à l'entrée comme à la sortie, **consigne déplacée de la page dans le composant** en `text-grey-40` avec l'icône `Pointer` (convient au tactile comme à la souris) ; **numéro du 16e décalé de 60 unités vers l'est** (`NUMBER_DX`) hors des hachures du bois de Boulogne, sans toucher aux centroïdes générés — décisions utilisateur. Note : `grey-40` (#a3a8b1) sur blanc ≈ 2,6:1, sous les 4,5:1 AA pour ce `text-xs` ; `grey-60` repasserait au-dessus si besoin.
- **2026-09-25** : jauge de la légende de la carte des arrondissements (`paris-map.tsx`) — style ui.sh **« Marches »** choisi parmi 21 (dégradé plein, hairline et point, trait fin, rail contouré, remplissage dégradé, tête blanche, segments, pointillés, fil et perle, piste blanche, rayures, double rail, traîne fondue, dégradé inversé, graduée, bloc plat, ruban, chevron, ton sur ton écartés) : **huit barrettes sable croissantes** (`STEP_HEIGHTS` `h-1/8` → `h-full`, `gap-0.5`, `h-3`) dont la copie foncée est **découpée à `--progress`** (`overflow-hidden` + largeur transitionnée `fillEase` 700 ms expo-out), les marches s'allument une à une avec le prix — décision utilisateur.
- **2026-09-25** : carte des arrondissements (`paris-map.tsx`), **trois améliorations UX** retenues sur une revue de huit (le filtre par intention « Je cherche pour… », puces par profil, a été construit puis **supprimé** le même jour, décision utilisateur) (décision utilisateur) : **clavier** = roving tabindex (un seul arrêt Tab sur la carte : l'arrondissement sélectionné, sinon le dernier focalisé, sinon le 1er ; flèches pour passer de l'un à l'autre en boucle 20 → 1, Début / Fin, Échap désélectionne, Entrée / Espace inchangés), consigne clavier en `sr-only` reliée par `aria-describedby`, **focus visible** = liseré foncé `stroke-primary stroke-[4]` dessiné sur le jumeau soulevé quand le focus vient du clavier (`:focus-visible`, repli sur le dernier type d'entrée) car la pièce de la boucle est transparente pendant qu'elle est éclairée ; **légende interactive** = chaque marche est un `<button>` d'une tranche de prix (libellé `districts.legend_band` « De X à Y € le m² : N arrondissements », zone de 24px de haut, visuel en bas), survol / focus = les arrondissements hors tranche passent à `opacity-25`, clic épingle (`aria-pressed`), second clic libère ; **résumé mobile** sous le puits (`lg:hidden`) = nom et prix du sélectionné + lien « Voir la fiche » (`#arrondissement-N`, `scrollIntoView` via `scrollBehavior()`), la fiche étant hors écran au moment du tap. Écartés pour l'instant : lien profond (hash), rangée de 20 pastilles, double tap pour zoomer, croix de fermeture sur la fiche. Tests `paris-map.test.tsx` (roving tabindex, légende, filtre, résumé, axe).
- **2026-09-25** : jauge de la légende — **bug** : le remplissage foncé s'arrêtait à mi-hauteur des marches. La copie foncée était dimensionnée sur une boîte de 12px alors que les marches claires, devenues des boutons de 24px (zone tactile), prenaient leurs fractions sur 24px. Le visuel clair est maintenant dans une boîte `h-3` au bas du bouton, même échelle que la copie foncée.
- **2026-09-25** : la consigne « Survolez la carte… » sous la carte des arrondissements **s'efface** (`opacity-0`, 300 ms) dès qu'un arrondissement est survolé ou sélectionné, en gardant sa ligne — décision utilisateur.
- **2026-09-25** : fiche de l'arrondissement (`district-detail.tsx`), colonne de gauche **au style des articles du blog** (décision utilisateur « un style un peu plus comme le blog ») : surtitre, nom, quartiers, puis **« À qui ça s'adresse » devient le paragraphe d'introduction** (sans titre, `text-base/7`), « Points forts » = h4 `text-xl font-medium` + liste à point primaire `before:` des articles, « Typologie de logements » = h4 + paragraphe, **« À retenir » = double carte sable** (`bg-background-08 p-2`, titre `text-lg` avec tuile icône `size-9` Lightbulb, carte blanche intérieure) comme la conclusion des articles ; les quatre rangées à tuiles d'icônes séparées par des hairlines sont retirées. Carte de droite inchangée.
- **2026-09-25** : carte de droite de la fiche d'arrondissement — le **prix au m² compte** de 0 à sa valeur à chaque ouverture de fiche (`StatValue`, 1,4 s expo-out, valeur finale en `sr-only`, direct en `prefers-reduced-motion`) — décision utilisateur « tu fais un effet ».
- **2026-09-25** : la ligne de source des prix (Notaires du Grand Paris) quitte le pied de la page Arrondissements pour **clore la colonne texte de chaque fiche**, avec l'icône `Info` devant — décision utilisateur. (Elle n'apparaît donc plus tant qu'aucun arrondissement n'est sélectionné.)
- **2026-09-25** : placeholder de la fiche d'arrondissement (`district-detail.tsx`, avant toute sélection) — variante ui.sh **« Squelette de la fiche »** choisie parmi 15 : à gauche des lignes grisées (`bg-background-05/08/10`, `aria-hidden`) qui reprennent la mise en page de l'article (surtitre, nom, quartiers, paragraphe), à droite la carte du site avec l'épingle `MapPin` sable et la phrase d'invite, même grille `lg:grid-cols-[3fr_2fr]` que la fiche pour que rien ne saute à la sélection ; écartées : tuile icône + surtitre, numéro sable 1-20, cadre pointillé, anneaux du site, liseré lumineux, trois repères chiffrés, bande sable, texte entre hairlines, chips des 20 arrondissements, flèche vers la carte, phrase Montserrat, conseil + clavier, aperçu deux colonnes — décision utilisateur.
- **2026-09-25** : cartes de quartiers Acheter / Vendre (`district-cards.tsx`) — **silhouette de l'arrondissement incrustée en bas à droite** de chaque carte (`components/page/arrondissement-glyph.tsx` : vraie forme de `lib/paris-arrondissements.ts` retrouvée depuis le numéro du nom « Paris 6e » / « Paris 6th », cadrée sur sa propre boîte englobante, remplissage `secondary-30` + hairline `secondary-60` non mise à l'échelle, `size-14`, opacité 70 % → 100 % au survol, décorative `aria-hidden`), la rangée de chips garde `pr-16` pour ne pas passer dessous — demande utilisateur.
- **2026-09-25** : silhouette des cartes de quartiers — **contour seul, sans remplissage, en grand** (`size-48`, `-right-10 -bottom-12`) qui **déborde de la carte et se fait couper** par son `overflow-hidden`, derrière le texte (`-z-10` sous `isolate`), opacité 60 % → 100 % au survol — décision utilisateur.
- **2026-09-25** : fiches d'arrondissement **détaillées** (`district-detail.tsx`, « détaille beaucoup plus les arrondissements… les métros, gares, restaurants, attractions… les universités, écoles aussi ») — chaque `ui.districts.items[]` gagne `summary` (résumé de 2-3 phrases, paragraphe de tête à la place du public), `metro` / `rer` (identifiants de lignes, identiques FR / EN), `stations` (gares, vide si aucune), `attractions`, `dining`, `parks`, `education` (≥ 3 lieux réels chacun, même longueur FR / EN) ; rubriques dans l'ordre Points forts → À qui ça s'adresse → Typologie → **Se déplacer** (badges métro = disques `rounded-full`, RER = carrés `rounded-none`, Montserrat sur la **couleur officielle de la ligne** : tokens `--color-metro-*` / `--color-rer-*` dans `tokens.css` (couleurs de marque RATP / IDFM, comme les logos sociaux), classes littérales dans `lib/paris-transit.ts`, composant `transit-badge.tsx` `role="img"` « Ligne 4 » / « RER B », gares en chips) → **À voir** / **Se régaler** / **Respirer** / **Étudier** (h4 avec icône lucide Landmark / UtensilsCrossed / TreeDeciduous / GraduationCap, listes de chips contour `border-border px-2.5 py-1 text-sm`) → À retenir ; **DTO `Arrondissement` + action `ListArrondissements`** (le contrôleur lisait `ui.php` à cru, contraire à la règle Domain/Content), `districts.items` ajouté à `SharedTranslations::EXCLUDE` (les 20 fiches voyageaient deux fois) ; tests `DistrictsPageTest` (lignes connues de la palette, FR / EN alignés), `ContentData` / `ContentActions`, `paris-map.test.tsx` (badges, gares, rubriques) — décision utilisateur. **Lieux et établissements à relire avant release** (règle « chiffres réels ») : ils sont exacts à la connaissance de l'assistant, pas vérifiés à la source.
- **2026-09-25** : colonne de la fiche d'arrondissement extraite dans `districts/district-profile.tsx` — variante ui.sh **« Fil vertical sable »** choisie parmi 20 : les rubriques (Points forts, À qui ça s'adresse, Typologie, Se déplacer, À voir, Se régaler, Respirer, Étudier) sont accrochées à un **fil vertical** `GradientHairline vertical` avec un **point sable** `bg-secondary-60 size-3` par rubrique, comme les valeurs de la page À propos ; le résumé reste en paragraphe de tête, « À retenir » en double carte sable et la source ferment la colonne ; écartées : rubriques numérotées sable, lieux sur deux colonnes, une carte par rubrique, surtitres et hairlines, accordéon, tuiles icônes, puits sable transports, listes à puces, rangées, résumé Montserrat, pastilles de comptage, points forts en tuiles, chips sable, grille libellé / contenu, compact, résumé en exergue, puits sable global, À retenir en tête — décision utilisateur. **Sur mobile la carte du prix passe en premier** (`order-first lg:order-none`) avant la longue colonne ; **source des prix en `text-grey-60`** (plus clair que le muted, dernier gris ≥ 4,5:1 sur blanc) — décisions utilisateur.
- **2026-09-25** : **résumés et points forts des 20 arrondissements réécrits** (`ui.districts.items[].summary` / `positives`, FR et EN) sur un modèle en trois phrases : **1) une phrase citable seule** qui pose l'angle de l'arrondissement (« Le 14e est l'arrondissement résidentiel et familial du sud de la Rive gauche »), **2) une phrase marché** (ce qui se vend, ce qui est rare, position des prix par rapport aux voisins, sans chiffre non sourcé), **3) les quartiers puis un repère daté** dont l'assistant est sûr (parc Montsouris 1878, Cité U 1925, Bourse de Commerce 2021, Notre-Dame décembre 2024, Philharmonie 2015, Station F 2017, RER E porte Maillot 2024…) ; les points forts deviennent **trois arguments d'achat** (décote vs voisin, rareté, transports, écoles) au lieu de redire le résumé ; les noms de lieux restent dans les chips — décision utilisateur « fais tout ça ». Dates à relire avant release avec les autres lieux.
- **2026-09-25** : page Arrondissements, **audit UX « fais tout ça »** — (1) **lien partageable** : la sélection s'écrit dans l'URL en `?arrondissement=14` (FR) / `?district=14` (EN) par `replaceState` (pas d'entrée d'historique par clic), lue côté serveur par `DistrictsController::selectedFrom()` (les deux paramètres acceptés, 1-20 sinon null) → prop `selected`, fiche déjà ouverte dans le HTML SSR ; le canonical reste la page nue ; une ancre `#arrondissement-N` (urls de l'`ItemList`) présélectionne aussi au montage ; (2) **défilement vers le bloc sélecteur + fiche** (`#districts-profile`, `scroll-mt-24` pour le header) après une sélection depuis la carte quand la fiche est hors écran (`scrollIntoView` via `scrollBehavior()`), jamais au chargement — viser la fiche seule tombait trop bas et cachait le sélecteur, retour utilisateur ; (3) **`district-picker.tsx`** au-dessus de la fiche = `Select` shadcn des 20 arrondissements avec leurs quartiers (alternative textuelle à la carte, pour qui ne connaît pas Paris et pour l'accessibilité) + flèches précédent / suivant (`aria-disabled` en bout de plage, « suivant » sans sélection ouvre la première fiche) ; (4) un sommaire de la fiche (liens d'ancre par rubrique), **supprimé le jour même** sur décision utilisateur ; (5) **prix contextualisé** dans la carte : « Prix n° 15 sur 20 · 48 % sous le plus élevé » (rang calculé sur les prix arrondis, « Le plus élevé de Paris » pour le premier) ; (6) **contact prérempli** : le bouton passe `?district=N`, `ContactController::show()` renvoie `prefill` {topic buy, message `contact.district_prefill` avec nom + quartiers} que `ContactForm` prend en valeurs initiales ; (7) un lien « Faire estimer mon bien à Paris 14e » sous le bouton, **retiré le jour même** sur décision utilisateur ; (8) **bouton contact répété en bas de colonne sur mobile** (`lg:hidden`, la carte étant passée en premier) ; (9) icône lucide sur les rubriques de lieux et transports, aucune sur les trois rubriques texte ; (10) **compteur du prix joué à la première sélection seulement** ; le bloc « Lire aussi » (articles de blog par arrondissement) attend un tag Sanity et n'est pas fait. Tests `DistrictsPageTest` (paramètre), `ContactFormTest` (préremplissage), `district-picker.test.tsx`, `tests/js/pages/districts.test.tsx` (URL, ancre), `paris-map.test.tsx`, `contact-form.test.tsx` — décision utilisateur.
- **2026-09-25** : carte du prix de la fiche d'arrondissement extraite dans `districts/district-price-card.tsx` — variante ui.sh **« Étiquette de prix »** choisie parmi 20 (« la 2 » d'abord, puis « la 20 pardon ») : le prix au m² dans un **chip blanc carré à liseré sable** `border-secondary-30 bg-card px-4 py-2` (comme le chip de date des pages légales), `text-3xl`, puis rang et écart, hairline, profil et arrondissement, bouton contact ; écartées : carte sable actuelle, prix en très grand centré, carte blanche épurée, carte sombre, jauge de prix, anneaux, tuiles 2×2, éditorial sans carte, numéro en filigrane, en-tête sable / corps blanc, nom en tête avec épingle, rangée de chips, prix et repères côte à côte, liseré lumineux, conseillers, compact horizontal, fiche d'identité en rangées, prix souligné sable, bouton bord à bord — décision utilisateur.
- **2026-09-25** : carte du prix (`district-price-card.tsx`), retours UX « le sens du prix / les faits / la version anglaise » puis « plus clair » — **format du prix par langue** (`districts.price_format` `:price €` / `€:price`, `per_m2` `/m²` / `/ m²`), **rang lu comme un acquéreur** : moitié chère comptée depuis le haut (« 3e prix le plus élevé de Paris »), moitié abordable depuis le bas (« 5e arrondissement le plus abordable »), extrêmes nommés (« Prix le plus élevé / le plus abordable de Paris »), ordinaux FR / EN par `lib/ordinal.ts` ; **écart nommé** « 33 % sous le 6e » / « 33% below the 6th » (nom court du plus cher, sans espace avant % en anglais) ; **rangée budget** « Budget pour 60 m² ≈ 870 000 € » (`BUDGET_AREA` 60, produit du prix arrondi, `toLocaleString` fr-FR / en-GB, note « À titre indicatif, sur le prix moyen arrondi ») ; les faits deviennent des **rangées libellé / valeur** : budget, **quartiers** (à la place du nom, déjà en titre), profil idéal en chip **suivi de la phrase « À qui ça s'adresse »** ; `dl` à groupes plats (`grid-cols-[1fr_auto]`, plusieurs `dd` par `dt`) pour axe. Le sommaire de la fiche (liens d'ancre) a été retiré le même jour — décisions utilisateur.
- **2026-09-25** : faits de la carte du prix (`district-price-card.tsx`) — variante ui.sh **« Rangées à icônes »** choisie parmi 5 : une **tuile d'icône lucide** `size-9` à liseré sable par fait (Wallet budget 60 m², MapPin quartiers, Users profil idéal), libellé `text-xs` à côté de la tuile, valeur `text-sm font-medium` et note (indicatif / phrase du public) indentées `pl-12` sous le libellé ; la tuile vit **dans le `<dt>`** (un groupe de `<dl>` ne contient que des `dt` / `dd`, règle axe `definition-list`) ; écartées : rangées libellé / valeur, blocs empilés, deux tuiles chiffrées, budget en avant — décision utilisateur.
- **2026-09-25** : page Arrondissements — la section **sélecteur + fiche** (`#districts-profile`) passe sur le **bandeau sable du site** (`from-background-05 to-background from-40%` en breakout `w-screen`, colonne `max-w-7xl px-6 lg:px-8 py-16 sm:py-20`), comme les avantages Acheter et les témoignages ; la carte blanche du prix et les tuiles ressortent sur le sable — décision utilisateur.
- **2026-09-25** : colonne de la fiche d'arrondissement (`district-profile.tsx`) sur **fond blanc** `bg-card p-6 sm:p-8` **sans bordure** (un liseré sable a été posé puis retiré dans la foulée), pour ressortir sur le bandeau sable de la section — décisions utilisateur.
- **2026-09-25** : prix de la carte d'arrondissement — **le compteur roule seulement sur la différence** : `StatValue` gagne une prop `from` (valeur de départ, 0 par défaut) et `district-detail.tsx` retient le prix de l'arrondissement précédent dans des refs réglées au rendu (`lastSelected` / `from`, stables entre deux rendus de la même sélection) ; la première sélection compte depuis 0, les suivantes de l'ancien prix au nouveau (remplace le « compteur à la première sélection seulement » du matin) ; test `stat-value.test.tsx` — décision utilisateur. **Espacements** : le `mt-8 sm:mt-12` de la carte CTA sous le bandeau est retiré (il s'ajoutait au `pb` du bandeau et au `gap` de la page : 240 px de blanc), la colonne blanche passe en `p-5 sm:p-8` pour laisser plus de largeur au texte sur mobile — décision utilisateur.
- **2026-09-25** : **« À retenir » des 20 arrondissements réécrit** (`ui.districts.items[].extra`, FR et EN) en deux phrases : la chose à retenir, puis un repère pratique pour l'acquéreur (les rues ou secteurs à viser, ce qui fait le prix, ce qu'il faut vérifier) — « plus détaillé sans trop en faire », décision utilisateur. Conseils à relire avant release avec le reste des fiches.
- **2026-09-25** : page Arrondissements — **le 6e est ouvert par défaut** (`DistrictsController::DEFAULT`, appliqué quand l'URL n'a pas de paramètre valide) : l'arrondissement de l'agence, le marché le plus valorisé, déjà celui de la démonstration de la carte ; la page montre d'emblée une fiche complète (contenu visible pour le référencement, prix + action dès le premier écran sur mobile). **L'URL n'est pas touchée** au chargement (le paramètre n'apparaît qu'après un choix), une ancre `#arrondissement-N` partagée prend le pas sur le défaut, un second tap désélectionne et ramène le squelette — décision utilisateur.
- **2026-09-25** : home — **le bloc central du hero est supprimé** (surtitre « 25 ans d'expertise… », h1 « L'agence de l'exceptionnel à Paris. », phrase-réponse, bouton « Contacter un conseiller » ; clés `home.hero_eyebrow` / `hero_title_1` / `hero_title_2` / `hero_text` / `hero_cta` retirées en FR et EN), décision utilisateur. Le hero garde le clip et la rangée des trois engagements, désormais en bas (`justify-end`, cascade dès 300 ms). Pour conserver un seul `h1` visible et le paragraphe-réponse GEO sous lui, **le titre du bloc de confiance passe de `h2` à `h1`** (« 500+ biens vendus à Paris. Un interlocuteur unique. » + `home.trust_text`, qui cite la marque et Paris). Tests `hero.test.tsx`, `trust-intro.test.tsx`, `HomeTest` adaptés.
- **2026-09-25** : home — **barre de recherche du hero** intégrée depuis Figma (712-25068 desktop / 712-25576 mobile) **sans logique** et sans page de résultats (décision utilisateur « tu intègres cela sans logique ») : `components/home/hero-search.tsx` = titre Montserrat « Conseil en immobilier de prestige » / « Luxury Real Estate Advisory » (en `<p>` : la page garde son `h1` dans le bloc de confiance) + carte blanche `p-2` à angles droits sans ombre (règles du site plutôt que les rayons Figma) ; dès `sm` un seul cadre `border-border` avec les quatre champs séparés par des hairlines verticales et le bouton plein `w-55` à droite, sur mobile les champs empilés chacun dans son cadre et le bouton pleine largeur ; ville / arrondissement (Select, 20 options `Paris 1er…20e` / `1st…20th`) et pièces (Select, 1 à 5+) via shadcn, surface et budget en `Input` numériques avec unité m² / € en `grey-40` ; libellés `sr-only`, `role="search"`, `onSubmit` = `preventDefault`. **Position** : en haut de l'écran sous le header sur mobile (`justify-between`, `pt-28`), juste au-dessus de la rangée des engagements dès `sm` ; cascade : barre à 300 ms, rangée à 420 ms. Icônes lucide (`ArrowUpRight`, chevron du Select) à la place des SVG Figma identiques. Clés `home.search_*` FR + EN, test `hero.test.tsx`.
- **2026-09-25** : barre de recherche du hero — **correction des champs** (décision utilisateur « plein de trucs qui ne vont pas ») : les cellules blanches de 40px peignaient par-dessus le cadre de 40px et laissaient des « crochets » aux bords ; les contrôles sont désormais **transparents** (`bg-transparent`, le cadre seul dessine les bordures), le cadre desktop passe à 48px (`sm:h-12`, bouton aligné à la même hauteur), les champs font 48px sur mobile en texte 16px (pas de zoom iOS) et 40px dans le cadre desktop, `focus-visible:ring-inset` pour que l'anneau reste dans le cadre, padding `px-4` uniforme, unités m² / € alignées à droite.
- **2026-09-25** : barre de recherche du hero, **sept améliorations** (revue « je peux améliorer quoi dans l'input ? », « ok go ») : libellé permanent au-dessus de la valeur dans chaque cellule (le placeholder n'est plus qu'un exemple : « Paris 6e, 75016… », « 80 », « 1 500 000 ») ; `name` + `autocomplete="off"` sur chaque champ, surface = chiffres seuls, budget **groupé par milliers à la frappe** (espace en FR, virgule en EN) ; cellule active / survolée = fond sable `bg-background-05` ; **croix d'effacement** (`Button ghost icon-xs`, `home.search_clear`) dès qu'une cellule a une valeur ; bouton **`outline` tant que rien n'est saisi**, plein ensuite ; **ville = combobox cherchable** (Popover + Command comme le sélecteur d'indicatif) sur les 20 arrondissements, filtrable par numéro, ordinal ou code postal 750xx, `input hidden name="city"` ; chevrons en `grey-40` comme les unités ; sur mobile **un seul cadre** avec des hairlines horizontales entre les cellules au lieu de quatre cadres, cadre desktop `h-14` avec hairlines verticales. Test `hero.test.tsx` (formatage, effacement, combobox, bouton).
- **2026-09-25** : barre de recherche du hero réduite à **deux critères**, ville / code postal et budget maximum (« pas besoin d'en mettre autant », décision utilisateur) ; pièces et surface retirés avec leurs clés `home.search_rooms*` / `search_area*`.
- **2026-09-25** : bouton de la barre de recherche = **bouton plein avec la seule loupe** (`Search`, `aria-label` = « Lancer ma recherche »), carré `size-14` sur desktop, pleine largeur sur mobile ; l'état `outline` tant que rien n'est saisi est abandonné — décision utilisateur.
- **2026-09-25** : barre de recherche du hero — **valeur des champs en `text-lg/7`** (18px) à toutes les largeurs, au-dessus du libellé `text-xs` (« le texte des inputs plus gros quand je tape dedans », décision utilisateur).
- **2026-09-25** : barre de recherche du hero **centrée** : bloc titre + cadre en `max-w-3xl mx-auto`, centré verticalement entre le header et la rangée des engagements dès `sm` (`flex-1 items-center` ; en haut sous le header sur mobile), titre centré à toutes les largeurs ; libellés et valeurs **restent alignés à gauche** dans les cellules (centrage essayé puis retiré dans la foulée) — décisions utilisateur.
- **2026-09-25** : carte de la barre de recherche du hero — style ui.sh **« Liseré sable »** choisi parmi 21 (carte blanche cadre gris, sans carte, verre dépoli, carte sable, pilule, cadre blanc bouton blanc, sombre, deux cartes, souligné, grande, compacte, bouton texte, bouton sable, cadre foncé, liseré lumineux, icônes, pastilles sable, bouton sous le cadre, double carte sable, minimale sur le clip écartés) : **carte blanche bordée d'une hairline sable** `border-secondary-30`, plus de cadre intérieur, cellules séparées par des hairlines sable (`via-secondary-30`), bouton carré plein collé au bord de la carte — décision utilisateur.
- **2026-09-25** : cellule « Ville / code postal » de la barre de recherche = **multi-sélection** : chaque clic dans la liste ajoute une **pilule** `rounded-full bg-background-08` dans la cellule (avec sa croix « Retirer … »), la liste reste ouverte, un second clic retire l'arrondissement, la croix de la cellule vide tout ; champs cachés `city[]` ; le bouton de la liste affiche « Ajouter un arrondissement » dès qu'une pilule existe ; le cadre et le bouton loupe grandissent avec les pilules (`sm:min-h-14`, `self-stretch`) — décision utilisateur.
- **2026-09-25** : cellule « Ville » — **pilule « Tout Paris » par défaut** (aucun arrondissement = toute la ville, sans champ caché), première entrée cochée de la liste ; le premier arrondissement choisi la décoche et la remplace, retirer tous les arrondissements (croix des pilules ou croix de la cellule) la fait revenir ; le placeholder « Paris 6e, 75016… » disparaît, le bouton de la liste dit toujours « Ajouter un arrondissement » — décision utilisateur.
- **2026-09-25** : pilules de la cellule « Ville » — **toujours sur une seule ligne** : un jumeau invisible de la rangée (toutes les pilules + la place du bouton « Ajouter ») est mesuré contre la rangée visible (`useLayoutEffect`, re-mesure au redimensionnement) ; s'il déborderait, seule **la dernière pilule choisie** reste, suivie d'un **badge « +N »** plein (`bg-primary`, `aria-label` listant les autres, `home.search_city_others`) qui rouvre la liste ; les pilules gardent l'ordre de sélection (plus de tri) — décision utilisateur. Test `hero.test.tsx` (largeurs simulées).
- **2026-09-25** : pilules de la cellule « Ville » (arrondissements, « Tout Paris », « +N ») = **le `Badge` outline du site** (bordure `border-border`, sans fond, `text-xs font-medium`, comme les badges du footer et du formulaire de contact) au lieu d'une pilule sable maison ; « +N » garde le même badge en bouton, survol sable — décision utilisateur « respecter le style de mon application ».
- **2026-09-25** : barre de recherche du hero **aérée** (« aère un peu les éléments ») : cellules `px-5 py-4 sm:px-6`, libellé / valeur `gap-1.5`, cadre `sm:min-h-18` et bouton loupe `sm:w-18`, pilules `gap-2`, éléments de la rangée `gap-3`, hairline verticale `my-4` — décision utilisateur.
- **2026-09-25** : liste des arrondissements de la barre de recherche — **ancrée sur toute la cellule** (`PopoverAnchor`) : elle s'ouvre **à la largeur de la cellule** (`w-[var(--radix-popover-trigger-width)]`), dans **le style du menu déroulant du formulaire de contact** (panneau bordé sans ombre, `p-1`, lignes `py-1.5 pr-8 pl-2 text-sm`, coche à droite) ; les icônes devant les libellés, ajoutées puis retirées dans la foulée — décisions utilisateur.
- **2026-09-25** : barre de recherche du hero — un second cran de padding (`px-6 py-5 sm:px-8`, cadre `sm:min-h-22`) a été essayé puis **annulé** dans la foulée (« reviens comme avant ») : les valeurs « aérées » (`px-5 py-4 sm:px-6`, `sm:min-h-18`, bouton `sm:w-18`) restent — décision utilisateur.
- **2026-09-25** : barre de recherche du hero, suite de la revue (« 1 2 3 » puis « 6 ok ») : **bouton « + Ajouter »** court sans chevron à côté des pilules (le repli « +N » arrive plus tard) ; **pied de liste** avec le décompte en direct (`role="status"`, `home.search_city_count`, « Tout Paris » sinon) et un bouton **« Terminé »** `outline sm` ; **montants fréquents** (1 M, 1,5 M, 2 M, 3 M, 5 M €) proposés dans un panneau ancré sous la cellule budget dès que le champ prend le focus (même style que la liste, focus conservé dans le champ, Tab / Échap ferment) ; anneau de focus sur la pilule elle-même (`focus-within:ring`). Le `Sheet` plein écran sur mobile pour la liste n'est pas fait. Clés `home.search_city_done` / `search_city_count` / `search_budget_shortcuts`, `search_city_more` = « Ajouter » — décisions utilisateur.
- **2026-09-25** : valeur des champs de la barre de recherche ramenée à la **taille normale des champs du site** (`text-base/7 md:text-sm/7`, comme `Input`) pour la cohérence avec la cellule ville (pilules et « + Ajouter » en petit) ; le `text-lg` essayé plus tôt est abandonné — décision utilisateur.
- **2026-09-25** : pilules de la cellule « Ville » **à angles droits** (`rounded-none` sur le `Badge` outline et sur sa croix), exception à la règle « badge = pilule ronde » pour cette barre — décision utilisateur.
- **2026-09-25** : barre de recherche du hero, **points 1 à 5 de la revue UX** (« fais tout de 1 à 5 ») : (1) **la ville se tape dans la cellule** — l'input est le combobox (`role="combobox"`, `aria-activedescendant`), la liste `role="listbox"` multi-sélection est maison (plus de cmdk) : « 16 », « 16e », « paris 6 », code postal **à partir de trois chiffres** (« 7 » ne doit pas matcher tous les 750xx), flèches, Entrée ajoute et vide le texte, Retour arrière sur texte vide retire la dernière pilule, Échap ferme ; clic sur une ligne sans voler le focus ; la cellule entière (pilules, croix, « +N ») n'est pas « dehors » (`onInteractOutside`) ; (2) **sous `sm`, les deux listes s'ouvrent dans un `Sheet` bas** avec leur propre champ (autofocus, clavier numérique), lignes plus hautes, « Terminé », `safe-area` — la cellule est alors `readOnly` et ne fait qu'ouvrir ; (3) **« N biens disponibles à Paris »** sous la carte, depuis `config('seo.listings_count')` ← `SEO_LISTINGS_COUNT` (chiffre réel seulement, ligne masquée sans valeur) ; (4) **suggestions** « Paris 6e / 7e / 16e / Moins de 2 000 000 € » qui pré-remplissent ; (5) **brouillon de session** `hooks/use-search-draft.ts` (`sessionStorage` `home-search`, restauré au montage, jamais écrasé par le premier rendu vide, brouillon corrompu ignoré). Tests `hero.test.tsx` (6 cas, dont largeurs simulées et `matchMedia` mobile) + `use-search-draft.test.tsx` — décisions utilisateur.
- **2026-09-25** : titre du bloc de recherche = ui.sh **« Titre en capitales espacées »** choisi parmi 21 styles du bloc (surtitre, filet sable, aligné à gauche, double carte sable, liseré lumineux, suggestions dans / au-dessus de la carte, carte sombre, verre dépoli, grande, compacte, cadre foncé, cellules numérotées, libellés en capitales, bouton texte sable, bouton blanc, liens texte, sans suggestions, panneau sable écartés) : Montserrat `font-semibold uppercase tracking-[0.2em]`, `text-2xl sm:text-3xl lg:text-4xl` — décision utilisateur. La carte est désormais un composant interne `SearchBlock` (ses refs et listes lui appartiennent), les critères restent dans `HeroSearch`.
- **2026-09-25** : titre du bloc de recherche = **« L'agence parisienne des clients internationaux »** / « The Paris agency for international clients » ; le sous-titre « Trouver le bien qui vous correspond vraiment » a été ajouté puis **retiré** dans la foulée — décisions utilisateur.
- **2026-09-25** : la **rangée de suggestions** (Paris 6e / 7e / 16e / moins de 2 M€) sous la barre de recherche est **supprimée** après une comparaison de 21 styles (libellé en capitales, puces seules, contour blanc, liens texte, points, pleines foncées / blanches / sable, libellés courts, flèche, plus grandes, empilées, hairline, bandeau, sable, coin sable, hairline verticale, verre, petites capitales, foncées en capitales) : aucune retenue — décision utilisateur. Clés `home.search_suggestion*` retirées ; seule la ligne « N biens disponibles » reste sous la carte.
- **2026-09-25** : titre du bloc de recherche — retour aux **tailles de la variante retenue** (`text-2xl sm:text-3xl lg:text-4xl`, `font-semibold`) mais **sans capitales**, avec un léger `tracking-wide` (l'agrandissement et le `0.12em` essayés pour la lisibilité sont abandonnés : « ne mets pas plus gros, reviens comme avant mais pas capitalisé ») — décision utilisateur.
- **2026-09-25** : titre du bloc de recherche à **l'échelle du h1 du site** (`text-3xl sm:text-4xl lg:text-5xl`, celles de l'ancien h1 du hero), toujours en bas de casse avec `tracking-wide` — décision utilisateur « trop petit ».
- **2026-09-25** : titre du bloc de recherche — **interligne élargi** (`text-3xl/11 sm:text-4xl/13 lg:text-5xl/16`, soit ≈ 1,45 / 1,45 / 1,33 au lieu de 1 / 1,1 / 1) — décision utilisateur.
- **2026-09-25** : le fond sable de la zone de saisie des cellules a été essayé puis **retiré** dans la foulée (« ok enlève ») — décision utilisateur.
- **2026-09-25** : barre de recherche du hero = ui.sh **mélange de « Libellés en capitales » et « Halo blanc »** choisi parmi 31 styles (carte blanche cadre gris, double carte sable, cellules détachées, sombre, verre dépoli, liseré lumineux, cadre foncé, bouton texte sable / blanc / contour, numérotées, libellés sable, compacte, grande, souligné, séparateur plein, pilules blanches / foncées, bouton large, bandeau en tête, carte sable, valeur soulignée, loupe sable, cellules espacées, titre à gauche, carte étroite, verre foncé, contour sur verre écartés) : libellés `text-[0.6875rem] font-medium tracking-wider uppercase`, carte bordée sable avec **halo `ring-4 ring-white/25`** ; le libellé « Ville / code postal » devient **« Arrondissement »** (FR et EN) ; **le placeholder est remplacé par un bouton « + Ajouter »** (badge sable, `aria-hidden` / `tabIndex -1` : affordance pointeur, le champ combobox reste la commande clavier) ; le pied de liste montre **une pastille ronde sable avec le nombre** d'arrondissements choisis, la phrase passe en `sr-only` — décisions utilisateur.
- **2026-09-25** : cellule Arrondissement — le bouton « + Ajouter » est **en texte seul** (`text-muted-foreground`, foncé au survol, icône `Plus`), sans badge ni fond ; la **pastille du décompte** passe en sable clair `bg-secondary-30` ; le **séparateur vertical** entre les deux cellules est un trait sable plein sur **toute la hauteur** (`w-px self-stretch`), plus une hairline dégradée avec marges — décisions utilisateur.
- **2026-09-25** : barre de recherche du hero — **découvrabilité** : « + Ajouter » reste dans le champ au repos, dès que le champ a le focus il laisse place au placeholder « Arrondissement ou code postal » ; **ligne de preuve sous la carte** avec des chiffres réels seulement : biens disponibles si `SEO_LISTINGS_COUNT` est renseigné, **« 500+ biens vendus à Paris »** (`about.stats`, prop `figure` passée de la page au hero) et **la note Google** (`seo.reviews`, logo + « Basé sur N avis »), rien n'est inventé — décisions utilisateur (points 5 et 3 de la revue).
- **2026-09-25** : **animations de la barre de recherche** du hero (« go » sur les six propositions) : cascade `hero-rise` titre → carte → ligne de preuve → rangée des engagements (pas 0 à 3, helper déplacé dans `components/home/hero-rise.ts`) ; pilule ajoutée en `pop`, retirée en **`pill-out`** (nouveau, 200 ms, fondu + 0.95, retrait immédiat en `prefers-reduced-motion`) ; panneaux arrondissements / montants en **`panel-in`** (nouveau, 4px + fondu, 200 ms expo-out) ; pastille du décompte re-montée (`key`) en `pop` à chaque changement ; loupe qui glisse d'un pixel en haut à droite au survol du bouton ; **un seul balayage lumineux** `sweep-shimmer` sur la carte 1,4 s après le chargement (`iteration-count 1`, `fill-mode both`, carte en `overflow-hidden`) — décisions utilisateur. Inventaire de `CLAUDE.md` mis à jour.
- **2026-09-25** : mobile — les panneaux de la barre de recherche **n'ouvrent plus le clavier** (« ça crée un merdier ») : la liste des arrondissements est un `Sheet` **classique** sans champ de recherche ni autofocus (`onOpenAutoFocus` neutralisé), la cellule est `readOnly` + `inputMode="none"` ; le `Sheet` du budget est supprimé, le champ garde le popover des montants à toutes les largeurs (le clavier numérique y est légitime) — décision utilisateur.
- **2026-09-25** : la **ligne de preuve** sous la barre de recherche (biens vendus, note Google, biens disponibles) est **supprimée** ; la variable `SEO_LISTINGS_COUNT`, la clé de config `seo.listings_count`, la prop `listings` et les clés `home.search_listings` / `search_proof_sold` sont retirées — décision utilisateur. Rien ne reste sous la carte.
- **2026-09-25** : header de la home **sur mobile / tablette** (sous `lg`) : **transparent au-dessus du clip** avec **logo et burger blancs** tant que la page est en haut et le menu fermé (`SiteHeader overlay` → `max-lg:bg-transparent max-lg:text-white`, `BrandLogo light` = artwork clair sous `lg`) ; dès le premier scroll ou à l'ouverture du menu, barre blanche et logo foncé habituels — décision utilisateur. Desktop inchangé (carte blanche flottante). Test `site-header.test.tsx`.
- **2026-09-25** : bouton de la barre de recherche — **sur mobile, un texte à côté de la loupe** : « Voir les biens » / « See the properties » (`home.search_cta_short`, `sm:sr-only` : icône seule dès `sm`), choisi pour dire le résultat attendu en deux mots ; **survol** : la loupe passe en sable (`group-hover:text-secondary-60`) en plus de son glissement — décisions utilisateur. (Le nom accessible reste « Lancer ma recherche ».)
- **2026-09-25** : burger du header mobile — **aucun état de survol** (`hover:text-current`, fond déjà transparent) : la variante `ghost` le faisait passer au foncé au survol, visible sur le header transparent de la home — décision utilisateur (« gris clair » envisagé, puis « rien »).
- **2026-09-25** : barre de recherche du hero **centrée verticalement sur mobile aussi** (`flex-1 items-center pt-24` à toutes les largeurs, la section en `justify-end`), au lieu d'être calée sous le header — décision utilisateur.
- **2026-09-25** : titre de la barre de recherche du hero **écrit mot à mot** avec `manifesto-in` (léger basculement 3D + flou qui se dissipe, 40 ms par mot à partir de 300 ms, comme le manifeste À propos et la citation des success stories) à la place de la montée en bloc `hero-rise` ; la carte et la rangée des engagements gardent leur cascade — décision utilisateur (« go » sur la piste 1 parmi quatre : espacement qui se pose, ligne par ligne, voile de flou écartés).
